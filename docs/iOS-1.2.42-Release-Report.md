# iOS 1.2.42（64）发布记录

状态：**1.2.42（64）已于 2026-09-20 15:13:39 +08:00 正式提交，版本与审核单均为 `WAITING_FOR_REVIEW`，审核通过后自动发布。**中国/中文与国际/英文均使用实际 iPhone 镜像和生产 API 验证。

## 基线与分支整合

- App Store 当前已上线 1.2.41（63），`READY_FOR_SALE`；保存的 ASC 读取见 `reports/ios-release-1.2.42/asc-inspect.json`。
- 1.2.41 实际源码不只包含旧报告中的 `febfeab`。已用当时补丁和源码逐文件重建，216 文件摘要与发布记录完全一致：`8670be15e6d5e31df8752192c9b694a81f5149c65a22ce2c0eeb429d75f3dd06`。完整发布增量与后续阅读修复固化在 `94499d2`，证据见 `published-source-baseline.json`。
- 本轮修复 `9935c6d`：微信读书跨页预读接续、完整解读字幕、EPUB 目录修复和 PDF 导入内存优化。
- 插图分支 `d4e427c` 通过正式双亲 merge `3e1d347` 纳入：EPUB 嵌套/重复/内嵌/SVG 插图、PDF 原页与 OCR 几何。保留新版目录索引、资源限制、取消检查、每页 autoreleasepool、2800 像素光栅上限；缓存版本提升为 4，升级重建旧派生内容而保留原文件及阅读位置。
- `0db3f26` 合入旧本地 `main` 历史。其 Google Drive 提交 `f8d2ed3` 已在已发 1.2.29 `334ce5a` 中整合并演进；逐文件核对后保留当前 OAuth、账户隔离、Limited Use 路由及去除未上线 Safari 扩展的发布实现，未倒退到旧文件。
- 统一候选分支 `codex/ios-release-1.2.42`，应用源码已于 2026-09-20 推送到远端 `main`、`codex/ios-release-1.2.42` 和 `codex/mobile-voice-explore-20260914`，三者均包含 `1ce1e56`。未来从远端 `main` 建立发布分支，运行 `scripts/build-voice-toc-integration.sh --check`；缺本批必备祖先即失败，不能仅复制功能文件。

## 最终候选与本轮发现

- 最终应用源码 `1ce1e560fd2aba335d18502f1c1992fbcd4f52d9`，304 个应用/资源/工程配置文件摘要 `635de9e4b189b11f866118824fa76af1a845caa10360a7e76d70a9b81fc30800`。逐文件清单见 `candidate-identity.json`；后续仅报告和证据修改不改变应用候选。
- 全量回归发现一处真实竞态：解读页结束安排异步翻页后，用户点击继续播放，旧任务与新任务可能同时使用后来的动画代次，造成一次结束触发两次翻页。`d165045` 在任务入队前固定代次，执行前再次校验，仅最新播放意图能继续；保留标注动画完成等待。
- 同轮另一个失败来自微信读书测试夹具把 compose 请求也返回为 SSE plan，导致重试并错误累计 plan 次数。夹具按实际请求路径分别返回合同数据，继续断言一次 plan、一次 compose，以及短章尾不混入下一页正文；不是绕过产品错误。
- 真机发现另一处暂停意图遗留：暂停朗读后切入解读，一次点击“开始解读”虽生成了音频，却继承旧队列的暂停标志，需要再点播放。`1ce1e56` 给明确的用户启动独立入口，在异步计划前接管/清理旧队列；自动翻页仍保留暂停保护，计划期间用户新按的暂停仍有效。三个新用例修复前 2 失败 / 1 通过，修复后受影响 52 项全部通过。
- 最终正常开发签名包于 **2026-09-20 13:49:34 +08:00** 安装成功，dylib UUID `3039C9C4-581E-3A0B-8077-8004DC15E216`。与应用候选 304 个文件逐一核对一致。修复前包的证据以 `before-start-fix` 保存，不冒充最终解读通过记录。
- 同源码 Release 编译完成，App / Share / Widget 均为 1.2.42（64），三个组件签名校验通过。该产物是开发签名 Release build，**不是 App Store 归档或分发包**。

## 已完成检查

- 合并后相关单测：42 项执行，38 通过、4 跳过、0 失败。xcresult `/tmp/CastReader1242Merge3.xcresult`，摘要 `merge-tests.json`。跳过项是外部 EPUB 语料、用户指定 EPUB、私有 PDF 导入及私有 PDF 冷重开样本；不能计为通过。
- 第一轮发现测试需要适配新合同：表格单元格明确分隔；PDF 高亮测试先设置当前段落以满足旧回调隔离条件。保留产品隔离检查，未移除断言。修复 OCR 恢复视口定位成功后应返回 true 的合并遗漏。
- 上述竞态修复后，24 项标注/翻页回归通过；完整受影响套件 48 项通过、0 失败、0 跳过。分别见 `ink-fix-tests.json`、`affected-final-tests.json`。同一用例可能重叠，不累加为独立测试总数。
- 初轮全量 1,845 项：1,832 通过、2 失败、11 跳过，两项失败已按上节修复。当时同候选全量复跑 **1,834 通过、0 失败、11 跳过**，见 `full-tests-final.json` 和 `/tmp/CastReader1242FullFinal.xcresult`；不将初轮失败隐去。跳过项包括外部 EPUB 语料、3 项需显式启用的私有文件回归、4 项显式实网回归和 3 项 StoreKit 授权测试；其中 3 项私有文件回归已在下述独立样本测试实际通过，其余仍记为跳过。
- `1ce1e56` 最终全量 **1,837 通过、0 失败、11 跳过**（共 1,848），见 `full-tests-after-device-fix.json` 和 `/tmp/CastReader1242PostDeviceFindingFull.xcresult`；受影响 52 项见 `start-intent-after.json`。
- 真实 EPUB / PDF 样本与相关 UI 共 6 项通过、0 失败、0 跳过，见 `import-ui-tests.json`。EPUB 的 40 条目录均指向有效段落；242 页 PDF 中 241 个有正文的页面全部保留、3,507 个段落，剩余一页为空白原页。
- 模拟器 UI 实际检查 EPUB 内嵌/SVG 插图、目录搜索/跳转/重开，以及扫描 PDF 原页高亮和冷恢复；已查看产物截图。它们验证渲染与恢复，不代替生产语音服务。
- 私有 PDF 缓存冷重开约 0.036–0.037 秒。本轮完整 PDF 导入在模拟器上为 99.49 秒、记录峰值约 786 MB，当时同时编译 Release；不得当作 iPhone 导入速度或内存结论。原始文件及正文不提交到仓库。
- Release 夹具排除通过：最终 Release 编译产物 123 个文件检查 54 个标记，Debug 正向对照成功、Release 无命中；九语编译资源检查通过。11 语商店文案 preflight 通过；公开产品/支持/隐私链接均为 HTTP 200，TLS 验证保持开启。
- 当前 ASC 元数据快照：11 种版本语言、11 种 App Info 语言、9 组共 45 张截图。本版仅计划更新 11 语 What's New，名称、副标题、现有描述/关键词/推广文案及截图保持原样。

## 核心服务门禁

实际设备为 iPhone 15 Pro Max / iOS 26.7，开发签名正常 App，未注入音频或假账号。服务路由在进程启动时切换；所有界面操作通过 iPhone 镜像。Release 的编译资源/签名/端点另行校验，不声称真机测试过 App Store 分发包。

| 地区/输出语言 | 朗读 Kokoro | 朗读克隆 vl/vc | 解读 Kokoro | 解读克隆 vl/vc |
| --- | --- | --- | --- | --- |
| 中国 / 中文 | PASS | PASS / PASS | PASS | PASS / PASS |
| 国际 / 英文 | PASS | PASS / PASS | PASS | PASS / PASS |

- 国际：`af_bella`、`vl_d1efb23a380bdaa2f6b0`、`vc_62934fe77f764051aa341b4b076bbb85`。最终包 13:53–14:04：英语 2,105 字符自有样本，24 个朗读段落；真实 QuickRead 6 块。暂停 Read 后明确启动 Explain，13:56:36.689 请求，13:56:45.743 开始实际播放，13:56:46.255 原文标注命中，后续块自动推进，无需二次 Play。社区→私人、私人→Bella 的解读音频均重新生成并播放；朗读私人→Bella 在 14:03:33.377 保持第 18 段，继续至第 23 段完成。首次未缓存解读仍有约 9 秒准备时间，不能表述为所有启动都无等待。
- 中国：`zf_001`、曹操 `vl_b4bb75aa2d3e56fd90a2`、私人 `vc_12e286594652455dae539cabbf93c2c0`。14:11–14:55，实际 TTS `api.castreader.cn`，QuickRead `quickread.castreader.cn`，501 字符样本产生 4 块真实讲解。三类声音均新生成、实际播放多段/多块，原文标注和完整字幕可见。另用 1,521 字符自有长样本验证播放中换声：14:35:48 私人→常规、14:36:49 常规→曹操、14:38:11 曹操→私人、14:40:43 私人→常规，均为 `auto=true`，当前位置接续；私人声音主动暂停后高亮保持，恢复后继续。
- 两区接口/真实播放推进证据：`global-final-api.log`、`global-final-progress.log`、`cn-final-api.log`、`cn-final-progress.log`。样本内容及哈希在 `core-samples.json`；可回溯请求 ID、实际播放状态、segment 完成和段落推进。实际界面已观察词/句段高亮、滚动、原文圈线标注；不把 HTTP 200 或 MP3 大小独立当通过。
- 额度：国际初始 3,501 秒，最终运行中观察到 3,237 秒；中国初始 7,200 秒，后续依成功克隆生成下降，换回常规未触发额度闸门，无未知余额误报耗尽。中国区原测试权益已过期，沿用用户授权新增零金额、无续费的 24 小时测试权益，到 **2026-09-21 06:06:35 UTC** 到期；原订阅和历史用量保留。本轮未重置用量。证据 `cn-test-entitlement.json`。余额观察不等于单独完成生产账单逐项对账。
- 本轮声音为 warm，没有实际遇到 `VOICE_PREPARING`；冷准备、旧响应、等待期间暂停、确认零余额/未知余额及取消所有权由确定性回归覆盖。实际暂停/恢复、读/解读互切和换声已在手机验证。持续运行无 watchdog/崩溃，界面可操作；本轮不提供校准的 CPU/内存基准结论。

### 微信读书补验

同一候选在《茶花女》第一章从封面进入，常规及曹操朗读自动跨页；快速多次下一页/上一页时，正在处理的翻页期间后续点击被 `busy` 门控，最终确认页面继续播放。随后单独上一页和下一页均确认，点击解读能重新出声和标注。

曹操解读连续两次自动翻页 14:49:17 与 14:49:43，预读都 `hit=Y`，页面确认耗时 0.267 / 0.196 秒；从上一页实际音频队列结束到下一页实际 `playing` 分别为 **375 / 306 毫秒**，见 `weread-transition-timing.json`。页面确认数字不等于音频间隙。手动翻页后全新生成解读另有一次 16.192 秒首音等待，期间正常完成计划与生成；不能宣称冷生成也达到亚秒。原文标注和完整句子字幕在镜像可见。本轮额外实测没有再次定位到特定短章尾页面；短页及动画代次用例已在最终完整套件覆盖，历史跨页修复保留。日志仅保存技术事件，用户正文和鉴权信息不入仓库。

测试结束正常启动 App，移除临时启动路由参数，恢复持久化的国际区；保留当前中文曹操、英文 Bella 选择，停止测试播放。

## 发布产物与送审结果

- 构建目录：`/tmp/CastReader1242Device`、`/tmp/CastReader1242Release`、`/tmp/CastReader1242Tests`，独立。归档目标 `build/CastReader-1.2.42-64.xcarchive`。
- 祖先门禁要求 `d4e427c`、`d165045` 及 `1ce1e56`，已通过；缺插图分支的旧候选反向校验失败。最终源码/配置与真机候选 304 文件完全一致。
- 归档签名/资源检查通过。Build 64 `526e2914-8834-4706-bc79-62fe41a5743d` 已达到 `VALID / APP_STORE_ELIGIBLE`，加密声明为 false。
- 已于 2026-09-20 15:07:23 +08:00 经 Xcode 官方流程上传，`EXPORT SUCCEEDED`。上传 IPA 42,944,125 字节，SHA-256 `8cf7a4d51170e9f1d3eb7cbb54e5a616ac6064c6eaa7e066809f1842caaf604a`，App / Share / Widget 均分发签名有效、`get-task-allow=false`。见 `uploaded-ipa-validation.json`。
- ASC 版本 `4a052397-9a76-46f7-8137-2998c92ad14c` 已创建，11 语仅更新 What's New，并回读确认其他版本文案一致；审核资料逐字段与已上线版本一致（仅保存布尔结果）。最终审核单 `c1501e0f-69a3-4238-b58f-2bac9818b971` 于 `2026-09-20T07:13:39.658Z` 提交；版本和审核单均回读为 `WAITING_FOR_REVIEW`，发布方式保持 `AFTER_APPROVAL`。

- 最终 ASC audit 无阻塞项，11 语名称/副标题保持，45 张截图全部 COMPLETE，未恢复旧截图；见 `asc-final-audit.json`、`submission.json` 与 `submission-readback.json`。构建上传与最终 App 源码未再变更，后续记录提交只更新文档和证据。

## 送审后新增的 Mac Safari 要求

2026-09-20 用户追加恢复 Safari，随后明确选择“仅 Mac 上的 Safari 扩展”。因此保留本次 iOS 审核单和 Build 64，不增加 iPhone/iPad Safari，也不因这项要求重新打包 iOS。

Mac Safari 使用扩展仓库 `safari-xcode` 的独立容器 `com.castreader.CastReader` / `.Extension`。本轮 App Store Connect 按该 Bundle ID 查询没有 App 记录；当前既有发布流程是 Developer ID 签名、公证、staple 后的 DMG/ZIP。网站源码的 Safari 下载入口仍为 `coming_soon`，没有有效下载 URL。不能仅修改入口状态或使用旧 1.2.18 包就宣称本次新版可用。

已将用户这次的 Mac 恢复分发要求、完整 iOS 主线提交 `1d35f89` 及保持现有 iOS 审核的边界同步到正在进行的任务「Safari 全功能对齐 Chrome 与 iOS 联合发布」（`01a0bcfa-6d0a-7cc3-ad2f-7ba0468febe1`）。以该任务最终 Mac 候选的真实验收、签名公证及下载回读为准；本记录不宣称 Mac Safari 已重新开放。

范围明确前准备的 `codex/ios-safari-release-1.2.42`（`fa7fdec`）仅为隔离的原生宿主合并准备，没有冻结 JS 资源、没有新验收、没有上传，**不纳入本次发布、不合入主线**。
