# 美区增长 · 「书库有声书」主轴落地方案

> 版本：v3（2026-08-16 收敛：新增 §12 循环验证协议，取代 §8 的分阶段路线；§5.6 分区定价与 §11 市场分层保留）
> **v3 收敛原则：所有动作围绕一个可测量的赚钱循环——进来 → 绑书库 → 留下来 → 按新价付费 → CAC < LTV。验证单位是循环整体配置，不是单个改动。**
> 目标：把美区主轴从「YouTube 转录」切到「把你已有的书变成有声书」，并同步修复定价与免费额度的变现结构。
> 适用范围：**美区（en-US）先行**；其他市场按 §11 分层推进——中文区不套用本主轴，各区价格按 §5.6 分区覆盖。

---

## 0. 一句话结论

产品已经把最贵的工程做完了（5 个书库绑定），但 App Store 叙事、ASO 词位、定价锚点三样全都没有对齐到它。本方案不新增任何核心功能，只做**对齐**。

---

## 1. 现状诊断

### 1.1 叙事与投入错位

| 层面 | 当前指向 | 工程投入 |
|---|---|---|
| App Store 描述第一段 | Listen to YouTube | 4 个 Service 文件 |
| 美区关键词字段 | video / caption / transcript / subtitle 占 4 位 | — |
| 首页核心文案 | 「把书架变成有声书」 | **5 个书库绑定，Kindle 单文件 11k 行** |
| 美区关键词中的书库词 | **0 个** | — |

最重的资产在 ASO 上零曝光。

### 1.2 ASO 数据佐证

来自 `docs/aso/keyword-corpus.md`（2026-08-05 全量导出，220 词）：

- **Top10 收割区：0 个词**
- 可爬区里躺着未被利用的书库词：`epub reader` #29(45)、`ebook reader` #35(45)、`book reader` #59(43)、`kindle reader for iphone` #131(53)
- `ai explain reader` #1 —— 自有词垄断，但无人搜索
- `eleven reader` #13(52) —— 竞品词有流量

### 1.3 付费意愿对比

| 主轴选项 | 用户搜索意图 | 免费替代品 | 价值锚点 | 付费意愿 |
|---|---|---|---|---|
| YouTube 转录 | 多为「下载视频 / 提取字幕」 | 极多 | 无 | **低** |
| 书库有声书 | 「我想听这本书」 | 少 | Audible $15–25/本 | **高** |

搜 `transcript` 的人和愿意为朗读付费的人，**不是同一批人**。

### 1.4 结构性风险

YouTube 提取依赖对方的 bot 检测策略（参见既往排查记录：拦截通常来自出口 IP）。把主叙事押在随时可能被切断的第三方链路上，风险不可控。

---

## 2. 主轴决策

**书库有声书做主轴，YouTube 降级为「输入源之一」。**

- YouTube 功能**不下线**，只是从描述第一段移到「BRING YOUR OWN CONTENT」列表里
- 关键词字段保留 `transcript` / `caption`（有真实流量），删掉 `video` / `subtitle`（意图不匹配）
- 后续版本更新说明不再以 YouTube 为主打

**一句话定位（美区）：**

> Your Kindle, Kobo, EPUB and PDF library — read aloud, in a real voice.

---

## 3. 叙事重构（美区 en-US）

### 3.1 名称

| | 文案 | 字符 |
|---|---|---|
| 现在 | `CastReader: Read Aloud & AI` | 26 |
| **改为** | `CastReader: Read Aloud Books` | 28 |

保住 `read aloud`（当前 #28、流行度 45，是现有最好的大词位置），新增 `books` token。

### 3.2 副标题

| | 文案 | 字符 |
|---|---|---|
| 现在 | `Text to Speech for PDF & Web` | 28 |
| **改为** | `Audiobook from PDF, EPUB, Web` | 29（原方案的 `& Web` 版本实测 31 字符超限，已改逗号连接） |

`audiobook` 进副标题是本方案的核心动作——它是整个价值锚点的锚，且当前在任何字段都不存在。

### 3.3 推广文本（170 字符内，可随时改不用过审）

```text
Turn the books you already own into audiobooks. Connect Kindle, Kobo, Google Play Books or O'Reilly, or open any PDF and EPUB, and listen with synced highlighting.
```

### 3.4 描述结构调整

段落顺序改为：

1. **YOUR LIBRARY, AS AUDIOBOOKS** ← 新的第一段，讲 5 个书库 + PDF/EPUB
2. **READ ALOUD — STAY FOCUSED** ← 保留，讲三位一体
3. **AI EXPLAIN — GO BEYOND THE WORDS** ← 保留，差异化武器
4. **BRING YOUR OWN CONTENT** ← YouTube 并入此处
5. BUILT FOR / 合规声明 ← 保留

新第一段建议文案：

```text
YOUR LIBRARY, AS AUDIOBOOKS
An audiobook costs $15–25. You already own the books. Connect Kindle, Kobo,
Google Play Books, or O'Reilly Learning — or open any PDF, EPUB, or document —
and CastReader reads them aloud in a natural voice, with synchronized
highlighting and automatic scrolling so you never lose your place.

You use your own accounts and content access. CastReader does not provide or
sell book content.
```

> 合规注意：`Kindle` / `Kobo` 等为描述性使用（说明兼容性），不得进入 App 名称。现有描述里的免责声明必须原样保留。

---

## 4. ASO 词簇重组（美区）

### 4.1 字段分配原则（沿用既有纪律）

同一 token 不跨字段重复；名称 > 副标题 > 关键词字段。

- 名称占用：`read`、`aloud`、`books`
- 副标题占用：`audiobook`、`pdf`、`epub`、`web`
- 关键词字段不再重复以上 token

### 4.2 关键词字段（100 字节上限）

**现在：**
```text
tts,video,caption,transcript,subtitle,voice,reader,ocr,scan,pdf,epub,article,dyslexia,study
```
（91 字节）

**改为：**
```text
tts,voice,reader,ocr,scan,article,dyslexia,study,ebook,listen,transcript,caption,library,text
```
（93 字节）

变更明细：

| 动作 | token | 理由 |
|---|---|---|
| 删 | `video`、`subtitle` | 搜索意图指向下载/提取，与付费点不匹配 |
| 删 | `pdf`、`epub` | 已移入副标题，避免重复 |
| 加 | `ebook` | #35，流行度 45，现有可爬区最高价值词 |
| 加 | `listen` | 组合词基础 token（listen to books / listen to pdf） |
| 加 | `library` | 书库叙事核心词 |
| 加 | `text` | 组合 text reader（#26，流行度 30） |
| 留 | `transcript`、`caption` | YouTube 仍有真实流量，降级保留 |
| 留 | `dyslexia`、`study` | 无障碍/学生人群，第二渠道伏笔 |

### 4.3 `kindle` token 的处理

**建议本轮仍不入关键词字段**，仅在 ASA 投放和描述正文中使用。

理由：`kindle reader for iphone` 流行度 53 确实诱人，但元数据被拒会阻塞发版；ASA 侧已能验证该词转化，先拿数据再决定是否冒险。

### 4.4 ASA 投放调整

新增 Exact 词组（建议 ¥12 档，与现有高档并列）：

```
ebook to audiobook
turn books into audiobooks
listen to ebooks
audiobook from epub
kindle to audio
kobo read aloud
```

原 ¥12 档中 `text to speech pdf` / `pdf text to speech` 两词若 4 周内仍零转化，降档让位。

---

## 5. 定价方案

### 5.1 现状与竞品

| 产品 | 月付 | 年付 | 折合月 |
|---|---|---|---|
| **CastReader（现在）** | **$4.99** | **$34.99** | **$2.92** |
| ElevenReader | $11 | $99 | $8.25 |
| Speechify Premium | $29 | $139 | $11.58 |
| Audible Premium Plus | $14.95 | — | $14.95 |

现价比 Speechify 便宜 4 倍。低价在美区不构成优势：

1. 传递「低质量」信号，与 Audible 锚点冲突
2. **LTV 过低导致买不起量**——这是 ASA 投不出去的重要原因之一

### 5.2 新价格（分两步走）

**第一步（本方案范围）：**

| 档位 | 新价 | 折合月 | 相对现价 |
|---|---|---|---|
| 月付 | **$9.99** | $9.99 | +100% |
| 年付 | **$59.99** | $5.00 | +71% |

保留 1 周免费试用（现有 `introductoryOffer` 配置不变）。

**第二步（观察 4 周后决定）：**

若转化率下降幅度 < 30%，继续提至月付 $12.99 / 年付 $89.99（对齐 Audible 与 ElevenReader 之间的位置）。

### 5.3 产品 ID 策略

**必须新建产品，不得修改现有产品价格。**

```
ai.castreader.pro.monthly.v2    $9.99
ai.castreader.pro.yearly.v2     $59.99
```

- 老用户继续持有 `ai.castreader.pro.{monthly,yearly}`，**永久保价**（grandfathering）
- 直接改价会触发 Apple 的订阅涨价同意流程，导致大量流失
- `ProManager.productIDs` 需同时包含新旧 4 个 ID；`isProProduct(_:)` 判定逻辑不变
- 新用户只展示 v2 产品；`Product.products(for:)` 请求全部 4 个，但付费页只渲染 v2

### 5.4 关于 Lifetime 买断

**不建议做。**

TTS 是持续变动成本（云端按量计费），买断用户会产生无上限的长期成本负债。若确实需要现金流，改做「3 年期」非续订订阅，而非永久买断。

### 5.5 定价页文案改造

`PaywallView.swift` 现有卖点列表：

```
无限朗读时长 / 无限解读次数 / 全部高级音色 / 最高 3x 语速
```

问题：全是**功能描述**，没有**价值锚定**。

改为在卖点列表**上方**增加一行锚点文案：

```text
One audiobook costs $15–25. CastReader is $5/month.
```

（中文版：`一本有声书 ¥100+。CastReader 每月一杯咖啡。`）

卖点列表调整为：

```
你的整个书库，全部可听    ← 新增，置顶
无限朗读时长
无限解读次数
全部高级音色
最高 3x 语速
```

### 5.6 分区定价（v2 新增）

$9.99 是**美区基准价，不是全球统一价**。App Store 同一订阅产品支持按 storefront 单独覆盖价格（900 价位点体系；ASC 里选基准国后可逐国手改）。v2 SKU 解决「老用户保价」，分区覆盖解决「购买力差异」，两者独立叠加，无需按地区增建 SKU。

| 市场带 | storefront | 月付基准 | 说明 |
|---|---|---|---|
| 美英加澳 | US / GB / CA / AU | $9.99 等值 | 主轴主战场 |
| 欧日 | DE / FR / IT / ES / JP | €9.99 / ¥1,200–1,500 档 | Kindle/Kobo 渗透高、Audible 贵，锚成立，不建议深折 |
| 中文区 | CN / TW / HK | 维持现价档 | 主轴不适用（§11.1），先不动价 |
| 拉美 / 印度 | BR / MX / IN | 当地惯例价（R$19.90 / ₹99–199 档） | 获客市场，变现次要 |

**成本红线**：TTS 变动成本全球一价，低价区重度用户可能烧穿 PPP 定价。当前体量可接受；低价区起量后再上成本闸（更紧免费额度或仅标准音色层），不要提前做复杂度。

---

## 6. 免费额度改造

### 6.1 现状问题

`QuotaManager` 当前配置：

```swift
let dailyListenLimit: Double = 1200      // 每日 20 分钟
let dailyExplainLimit: Int = 3           // 每日 3 次
```

午夜重置。**这个设计对付费转化是负向的**：用户永远「差一点点够用」，永远不产生购买冲动。这是 freemium 设计的经典陷阱。

### 6.2 改造方案：两层额度

| 层 | 额度 | 补充方式 |
|---|---|---|
| **首装赠额** | 朗读 **3 小时** + 解读 **10 次** | 一次性，不重置 |
| **月度维持额** | 朗读 **20 分钟** + 解读 **1 次** | 每月 1 日重置 |

设计意图：

- 首装 3 小时足够读完一本书的开头几章 → 产生「我想听完」的冲动，**在价值峰值处触发 paywall**
- 用完后月度小额度维持存在感，但明显不够用 → 持续的购买压力
- 「读完本篇」15 分钟宽限（`graceCapSeconds`）保留不变，避免中断体验引发差评

### 6.3 技术改动点

**客户端（`Services/QuotaManager.swift`）：**

- `dailyListenLimit` / `dailyExplainLimit` → 拆为 `onboardingGrant` 与 `monthlyRefill` 两组常量
- `rollIfNewDay()` → 改为 `rollIfNewMonth()`，并新增首装赠额的一次性发放与持久化标记
- `listenRemaining` / `explainRemaining` 计算改为「首装余额 + 月度余额」两段消耗，先扣首装
- 存储键需升版（否则老用户的每日计数会被误读为月度计数）

**服务端（`/api/pro/status`）：**

- 返回值需增加首装余额字段；现有 `listenRemaining` / `freeRemaining` 语义从「日剩余」改为「总剩余」
- **服务端与客户端必须同版本发布**，否则 `applyServerStatus` 会把日额度当月额度用

**迁移策略：**

- 已安装用户在升级后视为「已消耗首装赠额」，直接进入月度维持层
- 或（更友好）给老用户一次性补发 1 小时，作为改版补偿，降低差评风险

### 6.4 埋点

需新增/调整事件，用于验证改造效果：

- `quota_onboarding_grant_exhausted` — 首装额度耗尽时刻（这是新的核心转化触点）
- `paywall_shown` 需带 `trigger` 区分：`onboarding_exhausted` / `monthly_exhausted` / `pro_voice` / `speed_gate`
- 契约文件 `docs/analytics/mobile-events-v2.json` 同步更新，并跑 `scripts/verify_mobile_analytics_contract.rb`

---

## 7. 首页与入口调整

当前首页把 5 个书库入口与拍照/文件/网址等并列。主轴切换后需要区分权重：

1. **书库区上移至首屏第一屏**，标题从「书架来源」改为更有价值指向的文案（如「你的书，变成有声书」）
2. 未绑定任何书库时，首屏展示书库引导卡片，而非快速导入
3. 已绑定时，首屏展示「继续听」（最近书籍 + 进度），这是留存的关键动作
4. YouTube 入口移入 ➕ 快速导入内，不再占首屏位置

> 文案改动需同步九语 `Localizable.xcstrings`（每个 key 必须 9 个 locale 齐全），改 JSON 时用 `separators=' : '` 保持格式。

---

## 8. 实施路线

> **v3 注**：本节的分阶段节奏已被 **§12 循环验证协议** 取代——条目仍有效，但取舍、合批与时间线以 §12 为准（核心变化：阶段二/三合并为一个「配置 v1」发版，额度改造仅对美区新用户生效，老用户迁移与补偿作废）。

### 阶段一：零风险项（本周，不依赖发版）

- [ ] 推广文本改为书库叙事（App Store Connect 可直接改，无需过审）
- [ ] ASA 新增书库词组 Exact，原低效词降档
- [ ] 定价页锚点文案（若走远程配置则无需发版）

### 阶段二：元数据 + 定价（下个版本）

- [ ] 名称 / 副标题 / 关键词字段 / 描述结构，按第 3、4 节改
- [ ] ASC 新建 v2 订阅产品，配置 1 周试用
- [ ] `ProManager.productIDs` 纳入 v2；付费页只渲染 v2
- [ ] 首页书库区权重调整
- [ ] 九语文案补齐

### 阶段三：额度改造（再下个版本，需前后端同步）

- [ ] 服务端 `/api/pro/status` 语义升级
- [ ] `QuotaManager` 两层额度改造 + 存储键升版
- [ ] 埋点契约更新 + 三仓同步
- [ ] 老用户补偿逻辑

---

## 9. 验证指标

阶段二上线后 4 周读数：

| 指标 | 当前基线 | 目标 | 判定 |
|---|---|---|---|
| 美区 `audiobook` 词簇覆盖 | 0 词 | 进入 Top 100 | ASO 生效 |
| `ebook reader` 排名 | #35 | 进入 Top 20 | 词位提升 |
| 付费转化率 | 待测 | 下降 < 30% | 可继续提价 |
| ARPU | 基线 | **提升 > 40%** | 提价净收益为正 |
| 试用转付费率 | 待测 | 不下降 | 价格未超出承受区间 |

> 读数前提（R3）：阶段二必须配合 ASA 放量；无量情况下大词排名不动是应然，不构成方向证伪。判方向看中长尾组合词 + 转化率。

**决策规则**：ARPU 提升 < 20% 或转化率下降 > 40%，回滚至 $7.99/$44.99 中间档，而非退回原价。

阶段三上线后 4 周：

| 指标 | 目标 |
|---|---|
| `quota_onboarding_grant_exhausted` → `purchase` 转化率 | 建立基线 |
| 首装 7 日留存 | 不低于改造前 |
| 1 星评价中提及「额度」的比例 | < 5% |

---

## 10. 不做什么

明确排除，避免资源分散：

- **不做明星音色**：授权方要求逐脚本审批（平均 72 小时），与「用户自带任意内容」的产品形态根本冲突
- **不做用户声音克隆**：市场无成功先例；Apple Personal Voice 已免费内置同等能力
- **不做方言扩展**：除粤语外，中文方言无「朗读书面语」传统，属娱乐需求
- **不下线 YouTube**：功能保留，仅调整叙事权重

以上四项的完整论证见本轮讨论记录，不在此重复。

---

## 11. 市场分层与风险复核（v2 修订，2026-08-16）

### 11.1 主轴的地理边界

「书库→有声书」的价值 = **书平台自身不提供听书能力的缺口**，因此天生分市场：

- **缺口大**：Kindle iOS 端无 TTS（Amazon 保护 Audible）、Kobo 同类 → 美英德日加澳直接适用。
- **缺口小**：微信读书自带免费 AI 朗读，中文区用户无需第三方即可听书 → 中文区**不套用本主轴**，继续以解读/学习为主叙事；本方案的元数据改动不动 zh-Hans / zh-Hant。
- 结论：**不是「高价只能做美国」，而是「不同市场用不同主轴 + 不同价」**（价格见 §5.6）。美区先行是排序，不是放弃其他市场。

### 11.2 风险复核（对 v1 的修正）

| # | 修正 | 内容 | 对策 |
|---|---|---|---|
| R1 | audiobook 锚点的质量预期负债 | 锚价格可以，锚体验不行：真人演播的小说 TTS 无法替代，小说用户会失望差评。主轴真正成立在非虚构/学习/技术书。 | 文案红线：只说 "listen to the books you already own"，不承诺 narration 质量；BUILT FOR 段引导 nonfiction / study 人群。 |
| R2 | Kindle 曝光度风险（v1 最大遗漏） | 集成靠 WKWebView 抓 read.amazon.com（ToS 灰区）。从安静功能变成招牌，被 Amazon 针对（markup 对抗升级 / 法务函 / Apple 问询）的概率随曝光度上升。Speechify 公开打 Kindle 词多年未被封，说明容忍度存在，但其体量与法务承受力与我们不同。 | 主叙事用 "your library" 五平台并列，不单点绑 Kindle；`kindle` 维持仅 ASA 不进元数据；预设退路——Kindle 被掐后主轴仍由 EPUB / PDF / Kobo / GPB 支撑。 |
| R3 | 排名因果修正 | 「Top10=0 因定位不收敛」只对一半：元数据是相关性门槛，**排名引擎是下载速度**。改叙事必要不充分。 | 阶段二配 ASA 放量；读数看中长尾组合词与转化率，大词不动不构成方向证伪。 |
| R4 | 提价前置条件 | 转化率基线「待测」。漏斗若本身坏，提价只让数据更稀。 | 阶段一先铺 paywall trigger 埋点，拿 2–4 周基线，再切 v2 价格。 |
| R5 | 首装赠额可被刷 | 删装重来若重置 device_id → 无限 3 小时。 | 赠额以 Keychain 持久 id + 服务端记账双锚定；仅 UserDefaults 本地计数不可作为发放依据。 |

### 11.3 复核后维持的判断

明星音色不做（授权审批制与任意内容冲突）、用户自声克隆不做（Personal Voice 免费在位）、方言不做（除粤语外无书面语朗读传统）、YouTube 降级但不下线——均维持（§10）。

---

## 12. 循环验证协议（v3 收敛，美区 iOS 投放→付费）

> 所有动作围绕一个循环：**进来 → 绑书库 → 留下来 → 按新价付费 → CAC < LTV**。验证单位是循环整体配置（配置 v1），不是单个改动。目标：**7–8 周出裁决**。

### 12.1 五环与预注册通过线

| 环 | 严格口径 | 核心事件 | 预注册通过线 | 最早读数 |
|---|---|---|---|---|
| ① 进来 | 美区新装机（老用户排除） | `app_first_open` + 来源 | 喂量 ≥50 下载/日（不足由 Apple Ads 补） | 即时 |
| ② 绑书库 | 72h 内绑定任一书库 **或** 导入整本书（EPUB/PDF）；绑定单独细分 | `source_connected` / `book_imported` | 组合 ≥25%；绑定 ≥10% | D3 |
| ③ 留下来 | **听读留存**（非打开）：W1 ≥2 个听读日（日 ≥5min）；W4 仍有听读 | listen session | ②人群 W1 ≥40%、**W4 ≥30%** | D8 / D30 |
| ④ 付费 | 下载→试用：混合 ≥5%、**投放 cohort ≥8–10%**；试用→付费 ≥25%（$9.99/$59.99，7 天试用） | `trial_start` / `purchase` / `paywall_shown`(带 trigger) | 两条都过 | 试用 +7 天账期 |
| ⑤ CAC<LTV | Exact 词级每付费成本 ≤$45；首年净 LTV ≥$46 | AdServices 归因 + 收入 | 首年比 ≥1.0、含续订 ≥1.2 | 第 6–8 周 |

阈值规则：①②量级阈值允许第 1 周校准一次后锁死；③④⑤发版前锁死，不许事后改。

### 12.2 方法论纪律

1. **单配置整体验证**：小流量无多变量归因的统计功效，新价 + 新额度 + 绑定优先首页 + 新叙事一次发版；测量窗内配置冻结。
2. **阈值预注册**：读数只按 §12.7 决策树走。
3. **实验隔离**：额度改造走服务端区域门控，**仅美区新用户**；老用户保留旧价+旧每日额度（不进实验）→ §6.3 的迁移与补偿逻辑作废。

### 12.3 iOS 投放渠道判定（美区）

| 渠道 | 归因质量 | 判定 |
|---|---|---|
| **Apple Ads 搜索结果 Exact（中长尾）** | AdServices token：确定性、词级、无需 ATT 弹窗 | **实验主渠道**——小预算下唯一可严谨测量的渠道 |
| Meta/TikTok → App Store 直投 | SKAN/AdAttributionKit：粗粒度、延迟、小量触发隐私阈值丢数 | **不做**——测试预算下读不出数，且现价结构大概率倒挂 |
| Meta/TikTok → 网页收银台（落地页 → Stripe → 装 App → `serverPro` 解锁） | Stripe + CAPI：确定性 | **门后**——信息流唯一可行形态；管道现有架构已支持 |

只买 search results Exact；不买 Today tab / Search tab / Broad（意图不匹配纯烧钱）。

### 12.4 跑通的瀑布数学

净 LTV（小企业计划 15% 抽成后，年费占比 60%）：**首年 ≈ $46，含续订两年 ≈ $54–65**。

| 参数 | 必须达到 | 行业常态区间 | 抓手 |
|---|---|---|---|
| CPT（Exact 中长尾） | ≤$1.5–2.5 | $1–3 | 词竞争薄 |
| tap→install | ≥40% | 30–50% | CPP 对齐意图 |
| install→trial（投放流量） | **≥10%** | 5–15% | 意图匹配 + onboarding paywall |
| trial→paid | **≥30%** | 25–40% | 书库价值锚 |
| 年费占比 | ≥60% | 50–70% | 年费优先排版 |
| ⇒ 每付费成本 | **$50–65** | — | — |

**两个「跑通」的定义要分开：**

- **跑通到打平**（两年回本口径）：上表参数同时成立即可 ≈ 1.0–1.2x。概率中等偏高，这就是本实验判定的对象。
- **跑通到可规模化**（≤9 个月回本、LTV:CAC ≥2.5）：在 $59.99 年费下**大概率不成立**——由此，**第二步提价（$89.99/年）从"可选项"升格为"投放规模化的前提条件"**：④环转化若强，优先测的不是加预算，是提年费。

### 12.5 关键路径（发版门槛，8 项）

1. ✅（2026-08-16 客户端侧完成）埋点闭环：审计结论——五环事件基本已存在（`app_session_start`/`library_connection` 五平台 sync_completed 齐/`read_*`/`paywall_shown` 带 trigger 且额度触发已区分 listen_quota、explain_quota/`purchase_*`）；本次补齐 `purchase_result`+`entitlement_activated` 的 **offerType**（introductory/promotional/code/none，试用 vs 直购由此区分）与新事件 **ad_attribution**。①环新装机由服务端按 anonymousId 首见日期推导，客户端无需改。⚠️ 后端 `mobile-event-contract.ts` 是最终关口，**尚未同步**（另仓）：需登记 ad_attribution 事件 + purchase 两事件的 offerType，漏登记 = 静默丢数、实验作废。
2. ✅（2026-08-16）修绑定链路两个已知 bug：Kindle 开书跳登录页——新增 id_pk/id_pkel cookie 开书预检，缺失先静默预热书架（`openBookWithSessionPreflight`），反应式恢复保持兜底；Google Books 呈现等待门——三段超时兜底（残留 activeRoute 清理 / 8s 软门超时清残留标志 / 20s 硬上限放弃并打 `presentation_gate_timeout` 入漏斗）。真机回归验证待做。
3. ✅（2026-08-16 客户端完成）首页绑定优先：非 CN 区书库区（空态卡 + 五平台区块）上移到解读场景之前，YouTube 降位到场景区之后；空态卡文案改「你的书，变成有声书」+ 平台点名副标题；CN 区布局不变。模拟器冒烟通过；已绑定态排序与九语文案的视觉回归待做。
4. ✅（2026-08-16 客户端完成）v2 价格 SKU + 锚点文案：`ProManager` 增 `monthly/yearly.v2`（权益识别覆盖四 id），`displayProducts` 按 storefront 门控——**仅 USA 展示 v2，其余及未知 storefront 保守展示 v1**；付费页全部切 `displayProducts`；锚点行动态取年付月均价（不写死币种）；书库卖点置顶；Configuration.storekit 已加 v2 条目（$9.99/$59.99 + 1 周试用）。**ASC 手动步骤待做**：在同一订阅组创建 `ai.castreader.pro.monthly.v2`（$9.99）与 `ai.castreader.pro.yearly.v2`（$59.99），各配 1 周免费试用，非美 storefront 价格按 §5.6 矩阵覆盖（客户端虽不展示，仍建议设保守值防意外）。
5. ✅（2026-08-16 客户端完成）额度两层：`QuotaPolicy`（daily_v1 / two_tier_v1）由服务端 `/api/pro/status` 的 `quotaPolicy` 字段驱动，**未下发时行为与现状完全一致**（实验隔离在服务端）；two_tier 余额只由服务端签发，客户端 Keychain 镜像（删装仍在，防刷锚定）+ **fail-close**（镜像缺失按 0，绝不回落无限日重置）；扣减先赠额后月度，赠额归零时刻驱动 `paywall_shown` trigger 区分 `onboarding_exhausted` / `monthly_exhausted`（近 7 天内归零 = onboarding）。DTO 新增可选 `quotaPolicy` / `grantListenRemaining` / `grantExplainRemaining`。单测 QuotaTwoTierTests 5/5。**服务端待做**：/api/pro/status 按美区新用户下发 two_tier_v1 + 赠额记账（朗读 10800s + 解读 10 次一次性；月度 1200s + 1 次每月 1 日刷新）。
6. ⏳（2026-08-16 广告侧已配，等元数据解锁）Apple Ads：**关键读数——8/02–8/16 美区 Exact 全 campaign 仅 44 展示 / 0 点击 / ¥0 消耗，连 ¥30 出价的 Audible Intent 组也只有 1 展示**，组本身无 servingStateReasons → 卡在竞价层相关性：元数据没有 audiobook/book 词，Apple 相关性判分不放行。**阶段二元数据改版 = ASA 解锁的前提，同一件事**。已做：Audible Intent 组（含 kindle to audiobook 系词，此前已建）补入 6 个书库词（ebook to audiobook / turn books into audiobooks / listen to ebooks / audiobook from epub / audiobook from pdf / kobo read aloud，¥45 探地板价）；3 个最纯意图 kindle 词 ¥30→¥45。预算未动（US Exact ¥100/日封顶），跑通前不加钱。元数据发版后 72h 读展示。
7. 周度 cohort 看板：行 = 装机周，列 = 五环。仅此一张表。
8. ✅（2026-08-16 客户端侧完成）**AdServices 归因采集**：`AdAttributionService` 首启读 `attributionToken`（每设备一生上报一次，失败跨启动重试 5 次后记 unavailable），经 `ad_attribution` 事件上报。**服务端待做**：接收 token 并调 Apple 归因 API（api-adservices.apple.com）换 campaign/keyword 落库。

同车（不占关键路径）：元数据改版、YouTube 叙事降级、推广文本、**2–3 个 Custom Product Pages**（kindle 系 / audiobook-from-pdf 系各一，广告组一一对应，抬 tap→install 与试用率）。

门后：信息流（网页收银台形态）、第二步提价、featuring pitch、分区定价细调。

### 12.6 样本量与时间线

```
试用→付费 25% 读数 ±10pp → 需 ~80 个试用
80 ÷ 8% 投放试用率 = 1,000 下载（混合口径 ~1,600）
喂量缺口由 Apple Ads 补：每下载 $3–5 → 数据预算上限 $5–8k（买裁决时间，不是买增长）

第 0–2 周  开发 + 三仓埋点验证 + 发版
第 2–7 周  测量窗（配置冻结）
第 7–8 周  读数 + 按决策树执行
```

### 12.7 决策树（预注册）

| 失败位置 | 读数 | 诊断 | 动作 |
|---|---|---|---|
| ② | 组合 <15% | 流量意图或 onboarding 断裂 | 按词组拆 cohort；书库词人群也低 → wedge 获客假设受伤 |
| ③ | 绑定人群 W4 <20% | **价值假设失败（最严重）** | 停一切投放，回产品 |
| ④ | trial→paid <15% | 价格越界 | 回落 $7.99 重跑 4 周窗 |
| ④ | 15–25% | 时机问题 | 拆 trigger：`onboarding_exhausted` 显著更高 → 调赠额重测，不动价 |
| ⑤ | 转化过关但 cost/paid >$45 | 渠道贵 | 换词组/CPP，转化结论保留 |
| 全过 | ③④⑤达标 | **循环闭合** | 开信息流测试（网页收银台，$2–3k/月）+ 测 $89.99 年费 |

### 12.8 循环外仅保留两件事

1. 15 秒 Kindle 演示视频发 BookTok/Shorts/Reddit：测完播率与「求 app 名」评论密度，为门后信息流预验证素材。
2. 护栏指标：crash-free ≥99.5%、绑定尝试失败率 >20% 熔断排查、评分趋势、退款 <5%。
