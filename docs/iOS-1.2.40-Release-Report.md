# CastReader iOS 1.2.40 发布记录

本次发布将音色发现、普通/精选音色浏览与选择、朗读/解读音色同步、并发切换与额度误报修复，以及 EPUB 目录整合到同一发布基线。

## 源码与产物

- 发布工作树：`/Users/xuxuheng/Documents/.worktrees/CastReader-mobile-voice-explore-20260914`。
- 分支：`codex/mobile-voice-explore-20260914`。
- 应用源码：`5c45b75`，版本 `1.2.40 (62)`，App / Share / Widget 一致。
- 归档：`build/CastReader-1.2.40-62.xcarchive`。独立 DerivedData：`/tmp/CastReader1240Archive62`。
- 实际上传的分发包：`build/CastReader-1.2.40-62-AppStore.ipa`，42,704,409 字节；SHA-256：`b41caa6b6fb71453cd585a9d5307a7f747ce84c5a92634431bdaf30b62e6167d`。Apple Distribution 签名、App Store 描述文件及 `get-task-allow=false` 均已验证。
- 原 Build 61 归档未上传：送审回归发现滚动卡死后被 Build 62 取代，旧归档保留作核查。
- 发布基线脚本通过；保留 Kindle 恢复、更多/Aa/睡眠定时、书架、EPUB TOC、增长功能以及 `48998e7` / `f94b41d`。

## 送审前修正

1. 补全“清除”无障碍按钮的九语翻译。
2. 测试隔离：旧朗读测试读取模拟器持久化的克隆音色，污染 HTTP 音频夹具；改为每项测试设置普通音色并在结束后恢复，保留原断言。专题身份分组测试固定来源语言，避免与按语言推荐规则冲突。
3. 法语短音色发现页的滚动卡死：iOS 26.5 主线程持续约 99% CPU，采样期间物理内存约 166 MB，调用栈位于 SwiftUI 懒加载子视图放置与高度估算循环。内外两层纵向懒加载叠加固定分类头部触发问题。专题区域改为有界的 VStack，完整列表继续懒加载；原日/法语路径连续三轮通过。
4. 中区/国际区运营内容补全七种缺失语言译文。两区都回读确认 6 个推荐模块、7 个分类及 1,606 个音色；模块/分类标题具备九语。未改变音色成员、排序、封面、发布时间或额度。运营版本为 CN 8 / international 9，内容记录在后端提交 `4ede7835`。

## 验证

- 全量 iOS 单测初跑：1,770 项，1,746 通过、15 失败、9 跳过。15 项失败分别由测试音色污染和缺失译文引起，已修复；针对失败用例及受影响完整套件重跑 100 项，99 通过、1 跳过、0 失败。没有删除失败断言。
- Build 62 音色目录/推荐逻辑：48 项通过，0 失败。
- 九语小屏 UI：英语、简中、日语、西语、法语、德语、巴葡、意语、印地语，检查发现、浏览双 Tab、搜索、额度说明、44pt 试听目标和弹窗关闭。
- EPUB 目录与断点恢复 4 项、睡眠定时 5 项、中/日文离线保存与恢复 3 项界面回归通过。
- 音色完整 UI 套件在 Build 62 的本地用例通过；两项联网用例受临时 TLS/请求超时影响，修复网络连通后独立补验。日/法语真实试听补验通过；九语真实界面截图采集用例通过。印地语当前没有运营置顶专题，截图测试改为打开它实际存在的日常文章分类，未修改产品推荐或使用模拟数据。
- 9 项单测跳过包含 3 项需 StoreKit 授权的测试及 6 项需显式提供真实语料/在线参数的测试；未将这些计为通过。模拟器套件另跳过 1 项仅限连接真机的测试。
- 发布包九语字符串资源、186 个离线功能文案键、三个组件版本/签名核验通过；123 个文件扫描 54 个测试夹具标记无命中，Debug 阳性对照通过。Safari 扩展不在包内。

## 商店配置

- App ID：`6757636395`；版本 ID：`2e51ccae-4997-4a85-a3a3-541857cbddc8`。
- Pending App Info：`8a0b4ca4-06b9-45ce-b612-fe4e9c633a41`；原基线：`a55cc93e-30b0-4f98-86e1-c778d1ec47b1`。
- App Info 与版本文案均为 11 locale；标题、副标题、隐私链接完全继承。只修改本次版本描述和 What's New，保留空 Promotional Text。
- 中文与英文隐私政策、支持和营销页面均返回 HTTP 200。
- 内容版权声明、非豁免加密声明、价格、订阅、地区、年龄分级和 App Privacy 未改变。
- 审核联系方式/账号要求继承原版本，加入音色浏览、共享月生成额度及 EPUB 目录的审核步骤。
- 发布方式：`AFTER_APPROVAL`。
- 截图原配置：9 个 locale 各 5 张；繁中和墨西哥西语使用主语言回退。本次仅替换过时的第 5 张音色截图，保留其余产品截图和 App Preview。
- 最终回读：九种语言各 5 张，共 45 张截图全部 `COMPLETE`；其中 9 张音色页为本版本真实 UI 自动化截图。意语第一次替换上传长期停留在 Apple 处理中，在确认原图保留后移除该未完成替换并重新上传，最终处理成功且没有残留重复图片。

## 非阻塞观察

- 德语“我的声音”分段标签在固定宽度内由系统省略显示，控件可用、无障碍名称完整；正文与额度说明可正常换行。
- 印地语尚无运营置顶专题，现有分类和音色列表可用，本次未把未审核音色强行加入推荐。

## 提交结果

Build 62 已经 Xcode 官方通道上传，状态 `VALID / APP_STORE_ELIGIBLE`，Build ID 为 `595f5ccb-14fe-4b06-9b4c-d19dfdf318d9`。用户重新登录 Xcode 后解决会话过期，随后完成上传、构建绑定和正式送审。

- 提交时间：**2026-09-16 02:31:38（Asia/Shanghai）**，Apple 原始时间 `2026-09-15T18:31:38.049Z`。
- Review Submission：`c11f8320-d3dc-472e-8c88-927a3458efc0`，状态 **`WAITING_FOR_REVIEW`**。
- App Store 版本：`1.2.40`，状态 **`WAITING_FOR_REVIEW`**，绑定 Build **62**。
- 发布方式：**审核通过后自动发布**（`AFTER_APPROVAL`）。等待审核不等于已经上架。
- 送审前后均核对 11 个 locale、标题/副标题继承、截图处理完成、审核资料和构建资格。标题与副标题保持原样。
- 本次没有替换用户 iPhone 上的开发测试包，手机此前安装的应用源码仍是 `9f946ac`（Build 61）；商店候选应用源码是 `5c45b75`（Build 62）。此后的截图测试与发布记录提交不改变上传包。

完整证据位于 `reports/ios-release-1.2.40/submission-20260915/` 和 `reports/ios-release-preflight-20260915/`。测试原始 `.xcresult` 和构建日志在 `/tmp/CastReader1240*`；发布归档保留在工作树 `build/`。
