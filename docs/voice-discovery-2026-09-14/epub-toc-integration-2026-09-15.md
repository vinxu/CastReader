# 音色测试包补齐 EPUB 目录

此前安装的音色测试包来自 `633f6bb`，未包含 `codex/epub-toc-20260914`。`513c674` 是目录与平滑滚动实现，`ad4b885` 为后续 Android 对齐文档。已将整个分支合并为 `487c746`，该提交同时包含 `633f6bb`、`ad4b885` 与发布祖先 `64c2dbd`。

冲突仅涉及工程登记和本地化。工程保留音色文件并用 xcodeproj 注册新增 3 个应用 Swift 和 2 个测试文件；本地化只补 3 个新键。ReadAloudViewModel 自动合并后审核确认只增加目录跳转与生成时清除目录定位标志，当前克隆选择与生成逻辑保留。

解析缓存版本升级为 3，旧 EPUB 解析快照在重新打开时失效并从原始文件重建，保留原始 EPUB 和历史记录。阅读页底部的列表图标打开目录；目录跳转取消旧音频并保持暂停，保存所选章节供重开恢复。

## 本轮验证

- 105 项相关单元测试：103 通过，2 跳过，0 失败。覆盖目录解析、EPUB、缓存、滚动、阅读恢复与音色发现。跳过项为未配置的本地 EPUB 全语料扫描与缺少的 PDF 性能样本；没有重复宣称原分支 93 本语料为本轮实测。
- 新 UI 集成测试通过：现有 100 章 EPUB fixture 使用生产解析器与 ReaderHostView，开始播放 → 打开目录 → 搜索 Chapter 42 → 跳转 paragraph 82 → 旧音频清空并暂停 → 进程重开仍停在该章，目录入口仍存在。截图已目视检查。测试语音由现有本地 fixture 提供，不调用线上生成。
- `scripts/build-voice-toc-integration.sh --check` 与 `--device` 通过。新脚本强制核对发布基线、音色与目录三个祖先，避免再用单一功能分支覆盖真机。

证据：`/tmp/castreader-voice-toc-integration-20260915/`，包括 `regression.xcresult`、`toc-ui.xcresult`、截图、构建与设备回执。

## 真机包

2026-09-15 15:42 安装成功，iPhone (66)，版本 1.2.40 (61)，应用源码 `487c746`，Debug dylib UUID `E6B31ACF-67F9-31A5-BA3A-EAA5C9653CA1`。安装目录 `44F81F38-CD26-4811-8545-1AE20EDBF436`。随后设备锁屏，启动被 iOS 以 `Locked` 拒绝；本轮没有把模拟器 UI 验证计作真机导入验收。解锁并重新打开原 EPUB 后可测试新目录。

克隆切回常规音色的恢复卡住问题仍待修复。该问题诊断保存在 `/tmp/castreader-clone-to-regular-stall-20260915/diagnosis.md`，本次目录合并没有修改该恢复策略。
