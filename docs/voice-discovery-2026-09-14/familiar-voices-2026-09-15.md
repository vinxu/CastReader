# Voices close to you · 2026-09-15

发现页将自己录制和朋友提供的声音合并，按创建时间倒序显示最近 3 条。沿用个人音色的头像、显示名和来源，提供独立试听按钮、直接选用和选中标记；标题箭头进入完整 My Voices。没有可用声音时才显示录制自己 / 邀请朋友两个入口。保留月额度提示，个人声音不受发现语言过滤。

排序在 VoiceCloneStore 的账号音色列表变化时计算并缓存，最多显示三项。没有新增接口请求，不扫描社区目录，也不在试听状态变化时排序。授权已撤回、已过期及未就绪的声音不进入推荐；退出或切换账号清空缓存。缺失时间按服务端顺序稳定兜底，重复 ID 去重。

试听复用 VoiceClonePreviewPlayer 的预生成音频接口，选择复用 VoiceCloneStore.select 和 AppSettings 的多语言选择。保留 Pro、额度和账号检查；离开模块或切换语言时取消等待中的选择，并停止本模块的试听。

验证：

- 单元测试覆盖自己 / 邀请 / 赠送声音混排、时区与小数秒、重复与缺失时间、失效授权，以及刷新和账号切换后的缓存。
- UI 测试覆盖最多三条及顺序、选中状态、试听 / 停止按钮回调、查看全部、空列表恢复两个录制按钮。Debug fixture 不创建账号或上传录音，也不生成或播放真实音频。
- 原发现页空列表入口到登录 / 录制流程回归通过。
- UI 检查中修复了父容器覆盖子按钮 accessibility identifier，以及新增额度文案未匹配现有本地化键的问题。

测试记录在 `/tmp/castreader-familiar-voices-20260915/`。`tests-final.xcresult` 中两项单元测试和原录制入口用例通过；新布局测试的字符串大小写断言修正后在 `layout.xcresult` 通过，最终额度本地化复核使用 `layout-localized.xcresult`。没有改变朗读 / 解读引擎和线上运营内容。

真机：基线脚本 `--check`、`--device` 通过，并确认 `64c2dbd` 是当前分支祖先。2026-09-15 15:27 已安装至 iPhone (66)，包为 1.2.40 (61)，Debug dylib UUID 为 `8DE59A51-7685-335E-BC40-1DA28A2A5B3F`。设备处于锁屏，安装后的启动请求被 iOS 以 `Locked` 拒绝；本轮没有宣称完成真机试听。安装与启动回执分别为同证据目录下的 `device-install.json`、`device-launch.json`。
