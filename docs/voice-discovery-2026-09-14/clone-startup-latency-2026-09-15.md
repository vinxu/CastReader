# 克隆音色选择与首次出声优化 · 2026-09-15

## 实际瓶颈

旧真机包在 13:10:32.545 发出请求 `4D6A2980-0A4D-4A4B-B701-D60FB820F0C2-0`，13:10:43.038 收到响应，13:10:43.205 出声：网络与云端合计 10.49 秒，响应至播放约 0.17 秒。

美国 worker 同一请求的 GPU 排队 0.08 ms，合成 753.1 ms。此前该音色 `vc_library_ef4442c5b47684b0a096` 首次进入参考音频质量检查、降噪及特征构建：05:10:37.590 UTC 到 05:10:40.770 UTC，特征准备整体 3.179 秒，其中 DeepFilter 2.511 秒。其余时间包含参考音频下载、存储、鉴权与网络；没有完整分阶段时间，不能将十秒全称为模型冷启动。

## 客户端修改

- 社区音色选择复用当前账号 60 秒内已确认、未过期且仍有资格的能力快照。账号切换、额度耗尽、未知或过期时继续刷新；实际生成始终经过服务端鉴权与额度校验。
- 修复 `ParagraphSpeechGenerating` 的预取派发：原方法的默认参数与协议扩展使调用落到前台生成实现，修改 `currentRequestId`，可能取消正在准备的首段。协议现在包含明确的优先级，便捷入口完整转发，预取独立执行。
- 克隆请求独立调度：当前首段未返回时，后台预取暂缓；已有预取时，保留前台请求入口。等待期间切换账号会取消旧账号请求。
- 克隆前台请求先生成较短开头：中日韩最多 48 UTF-16 单位，其余语言最多 120；尽量按标点或空格切分。剩余文本完整保留，继续生成、入队；普通音色和已保存的续读分段保持原合同。
- 解读切换声音使用前台优先级生成新声音；已有讲解和缓存身份校验保留。

## 验证与安装

- 195 项测试通过：8 项调度、7 项真实 URLProtocol/播放器续读、59 项阅读恢复、121 项克隆音色测试。
- 原卡顿路径的 UI 回归通过：进入日常音色专题，反复试听、切换及返回发现；43.8 秒完成。
- 新的并发测试先复现了“后台预取取消首段”，修正协议转发后通过；续读夹具隔离了模拟器残留的社区音色设置，确保离线测试不访问付费 API。
- `scripts/build-reader-more-integration.sh --check` 与独立 DerivedData 的 `--device` 通过，保留发布祖先 `b6dd4d7`。
- 13:40:47 安装完成，13:41:24 工具启动成功。iPhone 15 Pro Max；1.2.40 (61)；安装目录 `6754BC13-7A9B-4C19-879D-695E17463371`；Debug dylib UUID `167AC857-CD48-3194-8572-EFD33FB1D384`。
- 真机包 `/tmp/CastReaderCloneLatencyDevice20260915/Build/Products/Debug-iphoneos/CastReader.app`。证据 `/tmp/castreader-clone-latency-20260915/`。

这些测试验证调用、顺序与文本完整性，不代表新的生产首音耗时。安装后的用户反馈及生产日志需要单独记录。长尾社区音色如果尚未被提前准备，首次使用仍可能触发云端参考音频处理；网络抖动也仍可能增加等待。
