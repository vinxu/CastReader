# 腾讯云账号与凭据说明

更新时间：2026-08-12

## 账号映射

| 腾讯云主账号 UIN | 用途 | 原始密钥文件 | macOS 钥匙串账号 |
| --- | --- | --- | --- |
| `100003464193` | CastReader 原账号；当前腾讯云资源迁出方 | `/Users/xuxuheng/Downloads/SecretKey.csv` | `100003464193` |
| `416166807` | Same 账号；CastReader 腾讯云资源迁入方 | `/Users/xuxuheng/Downloads/SecretKey (1).csv` | `416166807` |

两份凭据均已通过腾讯云 STS `GetCallerIdentity` 只读接口核验，调用者类型均为主账号 `Root`。

## 凭据保存位置

密钥明文不得写入本仓库、文档、聊天记录或脚本。两套凭据保存在当前 macOS 登录钥匙串中：

- 钥匙串 Service：`com.openai.codex.tencentcloud`
- 钥匙串 Account：对应上表的腾讯云主账号 UIN
- Secret 内容：包含 `SecretId` 和 `SecretKey` 的 JSON

可以在 macOS“钥匙串访问”中搜索 Service 名称定位。命令行自动化应直接从钥匙串读取到进程内存，不要把结果打印到终端、日志或 CI 输出。

原始 CSV 当前仍保留在下载目录，文件权限已收紧为 `600`（仅当前用户可读写）。钥匙串是长期保存位置，不应依赖下载目录文件一直存在。

## CastReader 迁移约束

- `100003464193` 是迁出账号，`416166807` 是迁入账号；执行任何释放操作前必须再次核对当前调用身份。
- 全球站/API 优先收敛到 Vercel + Supabase；腾讯云只保留中国区本地化所需资源。
- 迁移顺序必须是：目标账号盘点与容量确认 → 创建/复用低成本资源 → 数据和服务迁移 → DNS/客户端切换 → 健康检查与观察 → 最后释放原账号资源。
- 不能在目标服务尚未验证时释放原账号的 CastReader 网关、COS 网站素材或仍被客户端引用的资源。
- 两套均为 Root 密钥。完成迁移后，建议创建最小权限 CAM 子用户用于日常运维，并停用 Root API 密钥。

## 当前迁移方向

- 腾讯云迁出账号：`100003464193`
- 腾讯云迁入账号：`416166807`
- 全球站与标准 HTTPS API：Vercel。
- 全球数据库与公共对象存储：现有 Supabase 项目。
- 中国区本地化：`416166807` 的上海 CastReader CVM。
- 默认不再购买腾讯云弗吉尼亚 CVM；只有确认仍有不可迁移的旧 `8123` 客户端流量时，才考虑短期低配兼容节点。

## 2026-08-12 只读盘点结果

迁出账号原网关（已于 2026-08-12 释放）：

- CVM：`ins-jr4xlmvt`
- 公网 IP：`43.166.162.181`
- 地域：弗吉尼亚一区
- 规格：4 核 8 GB、约 100 GB 系统盘，按量计费
- 原职责：`api.castreader.ai`、TTS `8123` 入口、TTS 音色目录/样音、旧版文档兼容端点和外部美国 GPU 上游路由
- 服务形态：Nginx + 两个无持久挂载的 Docker API 容器；本机不是 GPU 训练或语音模型推理机器
- 近 14 天监控：CPU 平均约 4.3%、P95 约 7.6%；内存平均约 20.4%、P95 约 20.7%，现有 4 核 8 GB 明显高配

国际网关迁移结论：

- 当前客户端已使用 `https://api.castreader.ai`；远程 TTS 配置也已将美国节点改为标准 HTTPS，不再要求公网 `8123`。
- 现有弗吉尼亚 CVM 只是轻量 FastAPI/Nginx 网关，真正 TTS 推理由外部美国 GPU 提供。标准 HTTPS 代理与流式响应可迁移到 Vercel Functions。
- Vercel 已承接标准 HTTPS TTS、音色目录/样音与文档列表兼容端点；`api.castreader.ai` 已切换到 Vercel，并以正式域名实际合成音频及逐词时间戳验证通过。
- 原 CVM `ins-jr4xlmvt` 已通过腾讯云 `TerminateInstances` 释放；释放后 `DescribeInstances` 返回空列表。随实例计费的系统盘和公网地址同时释放，不再继续产生该实例的按小时费用。
- 不迁移旧 Docker 镜像、旧书、旧视频、训练音频或历史用户上传。

Supabase 整合状态（2026-08-12）：

- 公共桶：`castreader-public`。
- 已迁入并逐字节核验：647 个基础当前对象，共 `172223479` 字节；包括官网素材、当前语音目录/样音、远程配置和 Android 安装包。
- 另从仍实际存在的用户文档对象中迁入 210 个文件，共 `22973625` 字节，并将成功迁移记录的数据库 URL 改为 Supabase。
- 官网生产环境已切换到 Supabase 公共素材 URL；远程 TTS 配置使用 `config/tts-endpoints-v2.json`。
- 已清空 13 张旧书/书城/角色分析数据表，共 90478 行，释放约 354 MiB；保留表结构以避免遗留接口直接报错。
- 旧 COS 中已经不存在文件的 1686 条失效文档及其 27809 条段落已删除；当前保留 808 条可用文档，涉及 443 位用户。
- 旧书数据与失效文档合计删除 119973 行。当前功能所需的可用 `documents`/`paragraphs`、账号登录、订单订阅、Pro/额度、分享和实时会话数据均保留。
- 清理后 Supabase `castreader-public` 当前为 1293 个对象、`418597663` 字节（约 399 MiB）；数据库当前约 5.02 GiB，其中旧书核心表复核均为 0 行。

生产切换状态（2026-08-12）：

- `castreader.com` / `www.castreader.com` 已部署并切换到使用 Supabase 公共素材的 Vercel 生产版本。
- `api.castreader.ai` 的权威 DNS 已改为 Vercel CNAME；公共 DNS 与正式 HTTPS 请求均已验证命中 Vercel。
- 新网关已实测 283 个音色目录、英语/中文/日语及长文本 TTS、音频与逐词时间戳、音色素材和 `/documents` 兼容端点。
- iOS 与 Android 的远程 TTS/QuickRead 配置地址已改为 Supabase，避免再依赖腾讯 COS 配置桶。
- 原 `android.castreader.ai/app-release.apk` 下载地址已改由 Vercel 308 跳转到 Supabase APK；域名配置、TLS 与 17 MB 文件完整下载均已验证。

COS 清理终态（2026-08-12）：

- `ai-reader-1323065328`：已删除当前对象 12264 个、`8340419351` 字节（约 7.77 GiB）。主分页清理删除 4325299 个历史版本/删除标记、版本对象 `2451178490417` 字节；从头复核清理另删除 2361149 个残留版本/删除标记、版本对象 `2295651645285` 字节。两轮合计 6686448 个版本/标记、版本对象约 4.32 TiB。最终验证对象、版本、删除标记均为 0，随后已删除整个桶。
- `ai-reader-web-1323065328`：删除 169 个对象、`622384708` 字节（含明确放弃的旧视频），并删除桶。
- `castreader-android-1323065328`：删除 1 个对象、`17003635` 字节，并删除桶；APK 已迁移且旧域名下载兼容正常。
- `castreader-config-1323065328`：只为旧客户端兼容保留 2 个配置对象，共 485 字节。新客户端配置已迁移到 Supabase。

迁出账号资源终态复核（2026-08-13）：

- CVM：全地域 0 台。
- 轻量应用服务器：全地域 0 台。
- TDMQ（Pulsar / RocketMQ / RabbitMQ / AMQP）：上海、弗吉尼亚、硅谷均为 0。
- 独立 CBS 云硬盘：账单中出现的 4 块盘均已不存在。
- 弹性公网 IP：账单中出现的 3 个上海空闲 EIP 均已不存在；弗吉尼亚公网 IP 随 CVM 一并释放。
- CLS：全支持地域为 0。复核时发现硅谷遗留日志集 `global`（主题 `profile` / `audit`）；两个主题近 180 天均为 0 条日志，确认不属于当前 CastReader 运行链后已删除，并回读确认日志集为 0。
- TDSQL-C：旧弗吉尼亚 Serverless MySQL 集群已永久下线，复核集群列表为 0。

严格依赖边界：迁出账号不再承载 CastReader 的计算、数据库、云盘、公网 IP、消息队列或大对象存储。唯一明确仍与 CastReader 有关的资源是 `castreader-config-1323065328` 的 485 字节兼容配置；已发布的旧 Safari 扩展仍会读取它，因此在新版扩展覆盖率确认前不得删除。

迁入账号已有 CastReader 中国区本地化服务器：

- CVM：`ins-ldizyicl`（实例名 `castreader`）
- 地域：上海五区
- 公网 IP：`106.54.47.163`
- 规格：2 核 4 GB、50 GB 系统盘
- 计费：包年包月，当前到期时间 `2026-11-08`
- 购买用途：中国区本地化服务
- 当前职责：`api.castreader.cn` 的 Next.js 服务、Nginx 和本机 PostgreSQL
- 购买周期：`2026-08-08 14:57:45` 至 `2026-11-08 14:57:45`（Asia/Shanghai），整 3 个月
- 续费设置：`NOTIFY_AND_AUTO_RENEW`（到期通知并自动续费）
- 盘点时余量：约 2.6 GB 可用内存、42 GB 可用磁盘，整机负载接近 0

这台上海实例是中国区本地化专用资源，不应默认承担国际 TTS 网关迁移。只有在不影响 `api.castreader.cn`、容量验证通过，且上海到美国 GPU 上游的网络稳定性、`.ai` 域名境内接入要求和回滚路径全部确认后，才可评估复用。
