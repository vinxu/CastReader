# iOS 1.2.34 发布范围待确认

核对日期：2026-09-06。用户要求先确认范围，再打包、检测并提交 App Store；当前尚未归档或上传。

## 发布基线

- App Store 实时状态：1.2.33，READY_FOR_SALE，版本 ID `97e22d84-6451-4a1a-a67b-ae31a8367a52`。
- 最高已上传 Build：53；建议下一版 1.2.34（54），实际写入前再次回读。
- 无活动审核；当前 App Info：`042f6e8b-74c1-4858-aac2-022dc8db5652`。
- 正式源码基线：`codex/ios-release-1.2.33`，`e3bbe903c0bed886a53b3b0ba2c8240c4fe6b9d6`。
- 当前工作目录仍为 1.2.31（51），缺少正式分支中的邀请录音、上传音频创建音色、音色会话修复和首页布局等已上线改动。必须从正式基线建立发布整合分支，不能直接从当前目录打包。

## 建议本期纳入

1. **Kobo 登录、绑定与完整书架同步**：恢复白屏/加载失败，缩小底部引导，完善返回、刷新和授权弹窗处理；绑定完成同步到原生首页；多页遍历、去重及持久化；修复首次进入阅读器空白的有限恢复。
2. **Google Play Books 登录、授权与完整同步**：避免网页仍在加载就显示 0 本；根据实际页面变化推进扫描，适配真实卡片与虚拟滚动；修复登录轮询干扰扫描、取消弹窗后误跳转；检测筛选及未完整加载，避免把部分结果误认为完整书架。
3. **书架与账户状态保护**：账号切换、断开绑定及旧扫描回调隔离；同步失败保留已有书籍和阅读位置；完善重新同步、重新加载等多语言提示。
4. **普通音色朗读恢复**：短暂网络或服务错误有限重试；可选时间戳异常不再使有效音频整体失败，继续严格校验文本进度，避免漏读。来源 `09dcb64`。
5. **Kindle 翻页后续读恢复**：识别页面已经翻动但播放未接续的状态；只在确认翻页未发出时允许有限重试，避免重复翻页。来源 `814030a`。
6. **中国区线路与音色创建等待**：权威中国发行区域不因配置过期或不可达切去全球服务，启动不等待旧灰度配置；音色创建请求允许最长 150 秒等待，配合服务端处理。来源 `a1ee5f0`、`665ae0a` 的 iOS 增量。

已上线的录音/邀请/上传三种音色创建方式、邀请音色分类、登录保持、首页间距等全部作为基线保留，不重复宣传为本期新增。服务端部署和离线朗读独立分支不自动作为本期 iOS 新功能纳入。

### 历史修复归属补查

- 用户提到的“脑腐视频空链接”：对应 `readout-desktop` 仓库的浏览器扩展/桌面端资源迁移，不是 iOS 功能。已核对原任务记录：12 个原视频恢复并上传 Supabase，修复旧 COS 链接。
- 扩展发布分支 `codex/extension-compatible-1.2.36` 已包含 `5d335cfd674b1366b7db5e617a8f01a9098e75d0`（2026-09-04），迁移涵盖播放器、后台资源白名单和扩展权限；提交明确说明留待下一次扩展打包，不能据此声称已进入扩展商店安装版。
- 对 iOS 正式基线 `e3bbe90` 的 App、Share Extension、Widget 源码搜索，未发现该脑腐播放器或旧脑腐视频桶引用，因此无需将该扩展修复移植进本期 iOS 包。
- 本期 iOS 六项清单仍是待整合范围，不表示各独立分支已经完成合并；须在正式基线整合后逐项核对最终产物。

## 已有证据与剩余门禁

- Kobo：用户真机确认通过；两本真实书同步、打开、翻页及重启验证；四页 100 本模拟同步验证。详见 `kobo-shelf-sync-20260905.md`。
- Google Play Books：123 项单元/网页测试、4 条本地 UI 场景；真实两本书首页同步、逐本打开、翻页及重启；100 本模拟旧脚本 49 本、修复后完整 100 本。详见 `google-play-books-login-sync-audit-20260906.md`。未断言真实 TTS 播放，未推送 Google 修复版至真机。
- 普通 TTS / Kindle 恢复：独立分支 94 项聚焦测试通过；证据 `reports/backend-audio-iteration-20260906/release/validation-summary.json`。
- 当前目录静态 preflight 与 diff 空白检查通过：三个发布 Target 版本一致、11 个商店语种配置、凭据文件权限及 Xcode 工具正常。这不等于整合后的发布包已通过检测。
- 待范围确认后：整合正式基线及上述增量，解决冲突，核对 Release 不带模拟数据；先全量单元测试，再 UI 回归及关键真机验证；统一三个发布 Target 的版本；Release 归档、签名及产物检查。
- 按 SOP 上传后等待精确 Build 为 VALID / APP_STORE_ELIGIBLE；准备 11 语更新说明，保留现有标题、副标题与有效截图，核对审核资料；提交并回读版本与 Review Submission 为 WAITING_FOR_REVIEW 或之后状态。

## 商店中文更新说明草案

- 优化 Kobo 和 Google Play Books 的登录、账号连接与书架同步，修复大型书架只同步部分书籍的问题。
- 改善书架加载和重试提示，修复加载期间误报书籍数量及部分阅读页打开异常。
- 提升朗读及 Kindle 连续翻页的稳定性，改善短暂网络异常后的恢复。
- 改善中国区服务连接与音色创建的稳定性。

最终更新说明必须与整合后实际通过发布门禁的代码一致。
