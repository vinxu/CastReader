# Other bookshelf platforms: authorized live iPad acceptance

User authorized this follow-up on 2026-09-21 after synchronizing additional libraries. Continue using only `BFCF61DE-9C45-4467-8996-6F4E03AE7725`; retain all account, WebKit and library data. Baseline is `f2b5df6` on `codex/ipad-adaptation`.

Observed authorized shelves: Google Play Books 2 books, Kobo 2 books, WeRead 65 books. O'Reilly has no connected account record and zero cached books. No live coverage is claimed for it.

Work order: Google Play Books → Kobo → WeRead, with fixes and reviewed simulator evidence before moving to the next platform. Live Read/Explain audio and actual DOM ink are required; unchanged view models alone are insufficient.

| Platform | Status | Evidence |
| --- | --- | --- |
| Google Play Books | Passed | Core, long body, layout/search/narrow/native panels/contents, and strict natural Read/Explain continuation |
| Kobo | Passed | Real core, natural continuation, largest text/search/narrow/native panels/contents |
| WeRead | Passed | Real Read/Explain and strict physical continuation; largest type/search/load-more, compact resumed audio, Aa, and contents after reflow |
| O'Reilly | No synchronized book | No connected record; no live pass |

## Investigation

- The shared native mini player used by these platforms lacked the accessible expansion button and 44 pt targets present in Kindle. Added a real expansion button and iPad-sized play/close hit regions; its live UI verification is part of the first gate.
- Added opt-in, Debug-only observations of the actual bridge, player, view-model identities, page identity and visible native DOM highlights/ink. For Google's cross-origin reader, observations use the frame already validated for the native Aa command. They do not inject content, audio, marks, authentication or entitlement state.
- `platform-google-live-20260921T140340` and `platform-google-live-frame-20260921T140541` each skipped the single test: shell/SIMCTL_CHILD environment variables were not propagated to the XCTest runner. These are **not passes**. The runner configuration is being made explicit before running the gate again.

- `platform-google_books-20260921T140815` executed but failed at the first landscape check because the simulator stopped delivering rotation. Restarted the same UDID without erasing it; the next run passed full-shelf portrait/landscape.
- `platform-google_books-20260921T141433` reached the real book and successfully generated an HTTP 200 audio segment, but the test tapped pause while the existing automatic playback preference was loading. Fixed the test to observe the actual VM active/paused state before tapping; no user preference or production autoplay behavior changed.

- `platform-google_books-20260921T141851` proved real Read audio and visible word highlighting in portrait and landscape, then exposed a real defect: paused reflow replaced the page and cleared the highlight without restoring the anchored audio. `replaceLiveWebPage` now rebuilds that anchored paragraph with autoplay disabled.
- `platform-google_books-20260921T142400` passed that paused Read regression with preserved text identity/time and visible word highlighting. It also produced real Explain audio and ink in portrait, then exposed Explain treating viewport reflow as new navigation and clearing its plan/marks. The fix retains the immutable explanation scope and remaps it by provider source coordinates; natural continuation consumes the explained source range before starting newly visible content. Live verification is pending. Its native Google Aa panel was visible, but the lower controls were clipped in landscape; this is still open.

- `platform-google_books-20260921T143051` retained the Explain VM, mark UUID and exact paused audio cursor, but marks were not visible. Real Google chapter hashes changed across rotation, so source IDs alone were insufficient. Added a fail-closed, unique exact text-overlap remap for recreated source identities (including UTF-16 origin correction), plus bounded prior explanation slices for continuation.
- `platform-google_books-20260921T143553` verified native font-size increase/decrease and the clamped Aa panel bounds in landscape. The panel screenshot was reviewed; its remaining controls require a scroll gesture, which was added to the next live test. This run still had the source-ID-only mark issue and is not a pass. A script edited while its prior invocation was running ended with a shell parse error during report finalization; the completed xcresult and attachments were exported afterward, preserving the actual failed result.
- `platform-reflow-contracts-20260921T144017`: **4/4 passed** — paused reflow restores word/clock without unsolicited playback; late page arrival cannot override pause; explanation continuation excludes consumed prefixes; recreated source IDs require exact and unambiguous overlap.

- `platform-google_books-core-20260921T144132`: stopped at the explicit saved-position mismatch notice. The test now follows the product's requested paragraph selection rather than erasing progress.
- `platform-google_books-core-20260921T144651` and `145006`: later-page testing exposed Google changing its displayed page range on rotation, moving the speaking paragraph outside the viewport. The previous fallback restarted from the new page opening. Added exact source remapping and an owned, bounded adjacent-page correction only when matching neighboring source text proves the direction. The original TTS source and cursor are retained across clipping.
- `platform-google_books-core-20260921T145338`: Read rotation, paused word/clock restoration and native Aa font/line-height controls passed. Reviewed the paused highlight and landscape scrolled-panel screenshots. Explain retained its audio but its annotated paragraph moved to the adjacent page; the same source-aware reveal is now used for the latest explanation mark, including subsequent marks after a narrower reflow.
- `platform-google_books-core-20260921T145728`: the added exact-audio-source rotation assertion caught a legitimate segment completion during the test: cold resume started at 10.14/16.99 seconds, completed while rotating, and correctly continued into the next paragraph. The gate now explicitly selects a real paragraph before asserting same-segment rotation. The logs separately confirm saved paragraph/audio cursor restoration.
- Web bundle dependencies remain pinned to `2f060971136c61b3b7f407a155fbbb65ed43ce80` via `/tmp/CastReader-iPad-WebDependencies/src`. A local rebuild briefly used the changed sibling checkout; generated resources were immediately rebuilt from the pinned source before the next gate. Only the intended reflow command remains in the bundle diff.

- `platform-google_books-core-20260921T145938`: Read and Explain rotation passed, including identical narration text, paused clock, mark UUIDs and visible ink. Mini-player expansion exposed a second adjacent-page case: the new page shared no paragraphs with the immutable Explain scope but did share earlier neighbors from the preceding visual page. The bridge now retains that visual source window for directional evidence.
- `platform-google_books-core-20260921T150337` and `150634`: short paused paragraphs exposed duplicate filtering discarding the nil DOM reset before the same highlight command was restored. Moving duplicate filtering ahead of nil removal fixes re-painting; the accepted paused seek target also paints explicitly without depending on a periodic AVPlayer tick.
- `platform-google_books-core-20260921T150934`: that paused short-paragraph gate passed. The next failure was a short title in Explain: sentence-overlap matching deliberately required 24 UTF-16 units. A unique exact whole-paragraph match now handles short headings; ambiguous repeated headings and partial short matches remain rejected.
- Added regression tests for verified adjacent-page direction, invalid reflow stopping instead of restarting unrelated text, bridge delivery of the same paused word after a DOM reset, and frame/baseline ownership of geometry-only corrections. The dedicated contract runner suppresses real-book window restoration in its app host and excludes account-mutating payment tests.

- **`platform-google_books-core-20260921T151342`: 1/1 passed, no skips.** Real signed-in shelf portrait/landscape; Read audio, identical speaking source across rotation, paused clock/word restoration; native font and line-height changes with scrollable bounded panels; Explain audio and title ink, retained mark UUIDs/time across rotation; 44 pt mini-player expansion after another rotation; manual next page stays paused. Reviewed the landscape Explain and expanded portrait screenshots: the orange circle remains on the actual book title and all primary controls fit.
- Follow-up body gate uses the previously observed real `GBS.PP2.w.0.2.34_114` URL to explicitly exercise the long-paragraph/adjacent-page case; no fixture content, speech or authentication is supplied. Accessibility/narrow-window and natural continuation gates are still pending.

- **`platform-google_books-body-20260921T151719`: 1/1 passed, no skips.** The long-body page now passes the same full Read/Explain/rotation/pause/mini-player/manual-page gate. Reviewed both rotated and expanded screenshots: the same sentence about the Enchanted Isles is marked in the actual provider text in both layouts. The adjacent-page reveal preserves narration and returns the annotated paragraph after mini-player rotation.

- `platform-google_books-layout-20260921T152044`: largest accessibility text, dark shelf, search keyboard, rotation and sorting passed. Reviewed search and sorted-row screenshots: titles and progress wrap without clipping. The subsequent narrow-window home-scroll test overscrolled because nested accessibility frames used a different origin; the actual 375 × 497 window was valid. The revised gate enters the shelf before resizing and requires a nonzero minimum size and hittable shelf before continuing.

- **`platform-google_books-layout-20260921T153154`: 1/1 passed, no skips.** Largest accessibility text + dark search, sorting, 44 pt refresh, actual 375 × 497 system window, narrow shelf and reader, native font increase/decrease through the panel’s inner scroll area, full-window mini-player expansion, and native table of contents in both orientations. Reviewed narrow shelf/panel and landscape contents screenshots. A follow-up tightens the font-control scroll position so the entire button is visible in the evidence capture.
- `platform-google_books-continuation-20260921T153405`: test setup requested a nonexistent 4x option; the supported maximum is 3x. Corrected the test to use the real 3x control, preserving product speed options and restoring 1x at completion.

- `platform-google_books-continuation-20260921T153524`: executed 1/1 passing assertions, but log review found that the Explain page-identity assertion could also accept a geometry reveal. Real Read automatic continuation and Explain completion into a newly visible 182-character remainder were observed; this result alone is **not** accepted as an Explain automatic-page pass. Added Debug-only counts of actually issued automatic page turns and an immutable Explain-source hash. The strengthened gate requires a new automatic action, different page/source, playing real audio and visible new ink after rotation.

- **`platform-google_books-continuation-20260921T153940`: 1/1 passed, no skips.** Strict natural Read and Explain continuation now verified. Automatic Read action 0→1 commits at 15:41:15; Explain completes its original scope, starts the 182-character reflow remainder at 15:42:07, then issues the real next-page action and commits a new 1,526-character explanation source at 15:42:14. Automatic Explain action 0→1, new source hash, actual new-page narration and visible ink all satisfy the gate. Reviewed next-page Read and landscape Explain captures; speed restored to 1x.

- Contract regression: `platform-contracts-20260921T154522` ran 224 tests; 223 passed, with two assertions failing in one newly added highlight test because its fake Google main frame was correctly rejected by origin policy. Corrected that test to exercise the same production subscription via a valid Kobo main frame, without weakening origin checks. `platform-contracts-20260921T154817` then passed **70/70** (all Google source contracts plus that corrected bridge regression). This includes a newly added assertion rejecting repeated exact text within a single provider paragraph. All 224 selected contracts now have passing evidence across the two runs.
- Final narrow-panel refinement reduces only the dialog’s margins below a 480 px viewport height so more controls fit inside the smallest iPad window. Full-size reader pagination is unchanged.

- **`platform-google_books-layout-20260921T154926`: 1/1 passed**, final narrow panel CSS and scroll behavior, search/large type, native controls and contents rotations reverified. Google module cleared for the next platform.

- `platform-kobo-core-20260921T155403`: real Read, speaking-source rotation and paused word/clock passed. Rotating with Kobo’s native Aa dialog open exposed an indefinite native reflow cover that blocked the dialog; the run was interrupted and retained as a failure, not a pass. The reflow cover now has a three-second visual-only deadline after an initial readable commit; initial navigation and session recovery still require their original commit. Stable-page and narration checks are unchanged. Native WebView frame changes are applied without interpolated UIKit animations. Added a cover-visibility observation to the live word/ink gate, and regression coverage preventing a prior rotation deadline from revealing a new navigation.

- `platform-kobo-core-20260921T161028`: native Aa font controls and rotation/closing passed after the cover fix. Explain produced real audio and portrait ink, but rotation cleared ink without committing the new geometry. Identified polling restarting reflow settlement without its `forceExtract` flag. An unchanged final source signature was then incorrectly treated as a cancelled navigation. Refresh transactions now always publish their settled geometry, including a return to the same logical text.
- Reproduction `platform-contracts-20260921T161553`: the added real-WKWebView transient-slice/return-to-baseline test failed against the old adapter; the cover/navigation deadline regression passed. After the adapter fix, **`platform-contracts-20260921T161730`: 29/29 passed** (Kobo model/safety/recovery contracts plus the actual iframe reflow/mark test).
- `platform-kobo-core-20260921T161935`: a restored audio segment naturally finished during rotation (log: `0-1 → 0-2`), so the same-segment test assertion failed despite legitimate continuation. The core gate now selects a real Kobo paragraph before comparing rotation source identity, as already done for Google; cold-resume evidence remains in the logs.

- `platform-kobo-core-20260921T162244` / `162701`: metadata-only diagnostics identified a second real geometry defect. The full WebView viewport included 3–4-character slices from columns actually hidden behind Kobo’s narrower overflow container; the quality guard correctly rejected that corrupted snapshot, leaving no new geometry to paint. Extraction now intersects every CSS clipping ancestor before mapping into iframe coordinates. `platform-contracts-20260921T163055`: **2/2 passed**, including hidden-column exclusion and transient reflow/rotations.
- `platform-kobo-core-20260921T163212` reached the existing saved-position mismatch notice after the bounded provider-bookmark search. The requested paragraph selection did not clear it: the shared bridge listened only to the top document, while Kobo clicks occur in same-origin chapter documents. Registered paragraph listeners now follow each extracted owner document and accept only elements in the current native mapping. Added checks for actual iframe paragraph taps and rejection of a forged attribute on an unregistered hidden paragraph. No stored checkpoint or account was erased.

- `platform-contracts-20260921T163643`: **2/2 passed** with iframe taps and hidden-node rejection added. `platform-kobo-core-20260921T163753` then selected whitespace above the actual body on the book’s author page; the gate now chooses an observed, hittable long body text element instead of a fixed screen coordinate. Manual inspection also showed Kobo’s shell gesture surface receiving taps above the iframe; the bridge now resolves those taps against current visible text fragments, with a separate regression for this path.
- Manual same-simulator check at 16:43–16:48: the new 44 pt native contents entry opened Kobo’s actual table of contents, selecting Chapter Nine loaded its real text, Read produced orange word highlights, and normal playback advanced to the next physical page before being paused. This is supporting evidence; the complete automated rotation/Explain/narrow-window gates still must pass.

- **`platform-kobo-core-20260921T165400`: 1/1 passed, no skips.** Actual paragraph selection through Kobo's gesture layer, playing word highlights, rotation, paused word/clock identity, real font increase/decrease with the native dialog open during rotation, Explain speech and visible ink in both orientations, mini-player restoration and paused manual page navigation. Reviewed portrait/landscape ink and native appearance captures.
- `platform-kobo-continuation-20260921T165747` exposed a cold-opening checkpoint defect: a paused manual next page had advanced Kobo's bookmark beyond the stored listening position, while restoration only searched forward. Added a bounded nearby-backward phase before the existing forward search; it retains the saved checkpoint and requires an exact paragraph or uniquely verified hashed word context. Kobo now captures reflow context (strict text matching, no OCR correction). Account and provider storage remain intact. Temporary extraction diagnostics were removed after resolving the clipping issue.
- `platform-contracts-20260921T170246` executed zero tests because of a duplicate suite prefix in the selector; it is not a pass. The runner now normalizes that prefix and rejects zero-test success. **`platform-contracts-20260921T170416`: 3/3 passed**, covering both bookmark directions and strict Kobo reflow context.
- `platform-kobo-continuation-20260921T170655`: cold reopening restored the real checkpoint, and real Read automatically paginated with new-page highlights. Explain after rotation reached a previously unexplained remainder shorter than its minimum input, then stopped. The known-failing run was interrupted and is not a pass. Extended the existing bounded short-tail policy to Kobo and Play Books, and added owned, baseline-checked Kobo geometry correction so the current explanation source stays visible after reflow. No synthetic audio or content is used by the live gate.
- **`platform-contracts-20260921T171415`: 2/2 passed** — actual WKWebView Kobo viewport changes, stale-owner/stale-baseline rejection and geometry-only reveal, plus bounded short-tail continuation for Kobo/Google.
- **`platform-kobo-continuation-20260921T171558`: 1/1 passed, no skips.** Cold opening searched next → previous → previous and located the real saved paragraph, then Read automatically advanced with visible word highlighting. Explain retained its scope during rotation, completed the short remainder through the bounded policy, physically paginated and narrated a new 1,095-character source with visible marks. Reviewed the actual new-page portrait Read and landscape Explain screenshots; speed restored to 1x.
- Read-only account-cache verification also confirms O'Reilly has no connected record and both cached shelves contain zero books. It is outside available live-account coverage; no credentials were requested again.
- Kobo layout iterations `171941`/`172313`/`172608` preserved as failures: system search at maximum accessibility text hid the toolbar, then its keyboard covered the result, then its first menu tap dismissed active search. Added an inline sort menu for accessibility sizes and made the test dismiss the keyboard and wait for the actual menu before selecting.
- **`platform-kobo-layout-20260921T172836`: 1/1 passed.** Maximum accessibility type/dark search and sorting, actual 375×497 narrow shelf/reader using the second book *Falls From Grace*, 44 pt primary controls, native font increase/decrease, scrollable bounded settings, full-window mini-player expansion and table of contents in both orientations. Reviewed small-window settings, largest-type search and landscape contents captures. Kobo cleared for WeRead.

- WeRead `platform-weread-core-20260921T173320` first waited on the real cover. Play legitimately advanced through the cover into copyright; the live gate now invokes that existing opening flow when needed. `173658` and `175001` exposed actual rotation restarts: the provider briefly painted a partial landscape Canvas, then its final viewport contained fewer characters. Waiting for drawing alone did not preserve speech ownership.
- WeRead now keeps the spoken/explained source scope immutable across viewport or native font reflow, maps source character ranges into current Canvas glyphs, and uses bounded source-verified adjacent-page reveal when the active range moved out of view. Completion consumes newly revealed source before physically advancing; explicit page buttons, keyboard navigation and horizontal pointer gestures retire reflow ownership. Transient drawing is coalesced, and native WebView frames change without implicit animation. The cover's authoritative native table of contents can navigate before a text page has initialized.
- **`platform-contracts-20260921T175538`: 34/34 passed**, including WebKit/native-player availability, pause/rotation, transient partial-canvas protection and native Aa helper bounds across wide/cropped compact widths.
- `platform-weread-core-20260921T175735`: test assertions passed for actual first-chapter Read rotation, paused source/time, Explain audio/ink in both orientations, mini-player and paused manual next. Screenshot review caught the Aa panel remaining open through Explain because the test's dismissal tap was inside it. This is supporting playback evidence, **not final visual acceptance**; the gate now verifies actual native slider font changes and dismissal before explanation.
- **`platform-weread-continuation-20260921T180050`: 1/1 passed.** Real Read automatically continued into a new 415-character page with word highlighting; after rotation, Explain continued into a new 326-character scope with actual audio and 12 visible ink paths. Portrait Read and landscape Explain screenshots reviewed with no panel obscuring the body. Speed restored to 1x.
- `platform-weread-core-20260921T180413` verified the actual slider changed the Canvas font from 18 to 24, but the test's return tap selected 21 rather than the original first stop. Updated the gate to exercise the observed 18 → 21 → 18 stops and assert the font values, retaining the failed record.
- **`platform-weread-core-20260921T180838`: 1/1 passed, no skips.** Actual Read/paused reflow, verified provider font 18 → 21 → 18, rotated panel bounds and confirmed dismissal, actual Explain speech/ink in portrait and landscape, mini-player and paused manual next. Screenshots were reviewed with no settings overlay obscuring Explain.
- WeRead layout `181204` exposed iPadOS 26 collapsing the system search field; the shelf now explicitly presents its navigation-bar search drawer. `181356` verified large-type dark search/sort, 24 → 48 books through Load More, and a real 375×497 window, then exposed compact cold-resume matching only whole old paragraphs. The narrow native panel fitted; the test also tapped inside its 16-point inset instead of outside it. Both issues were corrected before accepting the layout gate.
- Added strict hashed context relocation for WeRead and optional 16/24/32/48-character context fragments for small pages. A target must match uniquely with at least 32 verified surrounding characters; no stored book prose, OCR tolerance or nearest-text guessing is introduced for these providers. Native table-of-contents navigation explicitly retires geometry-only ownership before choosing a chapter.
- Canvas-reflow fixture runs `platform-contracts-20260921T181932` and `182245` failed in fixture setup/final physical-page transition; fixed the fixture to retain the actual canvas while changing its provider source. **`182449`: 3/3 passed**, including immutable speaking scope, paused audio/marks, newly visible tail consumption without an extra physical turn, and the subsequent real next-page action.
- `platform-weread-core-20260921T182818` hit a legitimate automatic page boundary during rotation, so the test's within-page monotonically increasing segment-index assumption failed. The gate now selects the observed first chapter through the real TOC before timing a fresh reading session. This run also proved the Chinese service deliberately uses sentence timing without word timestamps; a word-only reflow cursor would not fix its compact restoration.
- Added sentence-based hashed relocation for untimed WeRead speech. Identical verified generated chunks retain proportional elapsed time; repartitioned audio replays the verified sentence boundary, and changed/ambiguous text is rejected. The capture uses the actual segment length rather than Chinese whitespace. **`platform-contracts-20260921T183204`: 3/3 passed**, covering compact relocation, untimed split/merge and identical-chunk timing.

- `platform-weread-core-20260921T183300` confirmed sentence-based checkpoints are now saved (`reflow=true`), then reproduced a different rotation race after a real TOC jump: the provider published its first partial landscape canvas before WebKit delivered resize. Extraction now also detects actual viewport changes before committing any paint, so event ordering cannot classify that partial canvas as a new page. The canvas regression now changes the real WKWebView width while deliberately delaying its resize listener.
- **`platform-contracts-20260921T183549`: 1/1 passed** with the real WebView-width/late-resize regression. `npm run test:kobo` also passed its actual DOM fixture (634 visible UTF-16 units from 6,950 total).
- `platform-weread-core-20260921T183652` then exposed the provider's no-op TOC case: selecting the chapter start already visible cannot produce a different page fingerprint. The bridge now requires a unique catalog heading AND its presence at the actual Canvas start before treating the selection as already satisfied; playback is retained and the TOC closes normally. A retained VM scope alone cannot authorize this, since reflow may have moved the visible page.
- **`platform-contracts-20260921T184225`: 1/1 passed**, rejecting missing, non-leading, ambiguous and non-authoritative chapter headings for the already-visible shortcut.
- **`platform-weread-core-20260921T184317`: 1/1 passed, no skips.** Final source now passes actual TOC selection, Read and paused source/clock rotation, provider font 18 → 21 → 18, bounded panel rotation/dismissal, actual Explain ink and audio in both orientations, mini-player, and paused manual next. Reviewed final portrait sentence highlight, landscape circled original text, and native font panel screenshots. Newly written Chinese checkpoints carry verified reflow context.
- `platform-weread-layout-20260921T184741` passed maximum-type search/sort, load-more, actual 375×497 controls/Aa bounds/dismissal, and cold resume located the saved sentence after three observed pages (`oldPara=1 → newPara=0`). It then hit the same already-visible TOC case after expanding: the retained narrow VM scope did not contain the chapter heading, although the new visible Canvas did. Removed that VM precondition; the unique authoritative catalog and actual visible Canvas remain mandatory. The final layout gate now plays the restored compact-page audio and navigates to the observed introduction and back, requiring two actual changed page identities.
- `platform-weread-layout-20260921T185322` verified real compact resumed audio at the saved 3.478-second sentence position, visible highlight, paused controls, bounded Aa, and the actual TOC navigation to the introduction. Its final assertion incorrectly required another page change even though the requested first chapter was already shown in the right column. `platform-weread-contents-20260921T185950` confirmed that rejecting this as an already-visible selection causes the provider's catalog no-op to time out. Final behavior follows the existing active-catalog-row convention: selecting an authoritative unique chapter heading already present on the stable visible Canvas closes the catalog and preserves the page/playback. It never forces an extra physical turn. Real navigation to the introduction still requires a changed page; selection of the visible right-column chapter is checked separately with visible text evidence.
- **`platform-weread-contents-20260921T190423`: 1/1 passed.** Actual introduction navigation after rotation and already-visible right-column chapter selection both complete. Reviewed the final two-column screenshot: catalog dismissed, actual requested chapter visible, native controls unobscured.
- **`platform-weread-continuation-20260921T190731`: 1/1 passed.** Final stricter gate requires an accepted physical Explain page after rotation, excluding semantic reflow tails. Actual Read automatic action 0→1 and Explain physical-page count 0→2 were observed; new-page source and ink were required while audio was playing. Reviewed the landscape next-page underline. At 3x, the later accessibility/metrics attachment can observe the following page transition after the screenshot; the live predicate and screenshot capture precede that transition. Playback speed restored to 1x.

- **`platform-weread-layout-20260921T191135`: 1/1 passed, no skips.** Final complete layout gate: largest accessibility text/dark search and sort; 24 → 48 Load More; real 375×497 shelf and reader; exact restored sentence playback and visible highlight; 44 pt controls; bounded native Aa open/dismiss; expansion and both-orientation TOC; actual introduction navigation and current visible chapter selection. Reviewed the final compact playing highlight, scrollable Aa panel and large-type search/keyboard captures. WeRead is cleared; final shared contracts and signed Release artifact verification follow.

- **`platform-contracts-20260921T191627`: 269/269 passed, zero skips.** The seven account-preserving suites cover Google/Kobo/WebKit extraction, resume, WeRead opening/availability and appearance. This precedes the final short-sentence fix below; a fresh complete run is required.
- The first normally signed Release smoke check at 19:21 exposed a real reverse-resize issue: the latest 53-glyph compact WeRead page saved `reflow=false`, then a full-screen cold reopen exhausted the bounded search and showed the saved-position notice. The old reflow validity rule counted only text outside an untimed Chinese sentence, discarding reliable context when that sentence occupied most of a small page. This Release attempt is **not** final acceptance.
- Untimed sentence checkpoints now count the fully hashed sentence toward at least 32 exact semantic characters, with unique matching and verified available neighboring context; word-timed checkpoints and Kindle OCR retain their original rules. No source text is stored. Changed text, duplicate matches and insufficient total context remain rejected. **`platform-contracts-20260921T192510`: 2/2 passed**, covering compact-to-wide short sentences and generated-audio repartitioning. A focused real-book cold-reopen gate exercises both directions before the replacement Release candidate is accepted.
- `platform-weread-resume-20260921T192944` failed during initial preparation: the catalog closed before its replacement Canvas committed, and the test started playback against the outgoing snapshot. The focused gate now selects the observed introduction, uses the actual next-page control, and waits for a stable committed body before listening. This failure does not count as a compact/full-screen restoration pass.
- `platform-weread-resume-20260921T193412` failed while preparing a catalog jump: the provider rendered its visible “加载失败” page, and the app correctly reported `visible-error`. The final resume-specific setup instead selects an actual observed body paragraph after the initial reader settles, creating a new genuine listening checkpoint without catalog navigation or History mutation. Catalog behavior retains the separately passing contents gate; this failed request is not represented as a pass.
- `platform-weread-resume-20260921T193802` failed because XCTest could not compute the Canvas text's accessibility activation point; the requested paragraph was not selected. The final setup uses a confirmed native page turn to choose a new location, respecting the reader's mismatch notice.
- **`platform-weread-resume-20260921T194650`: 1/1 passed.** Actual full-screen Read → process termination → real 375×497 reopening and Read → termination → full-screen reopening and Read. The compact checkpoint saves `reflow=true`; the final full-screen paragraph is relocated (`oldPara=0 → newPara=1`) with no mismatch notice and real audio/highlight. Reviewed compact and wide screenshots. Separate manual review confirmed that a portrait catalog selection of the first chapter currently renders the preceding introduction; this additional target-position issue is being investigated before final delivery.
- Manual comparison at 19:52–19:54 verified that the platform's own visible title control navigates correctly. The adapter had dispatched the synthetic semantic click to the enclosing `li`; it now dispatches **one** coordinate-bearing click to the actual chapter title/action control, allowing the site's delegated target handling to select the correct chapter. Temporary row/HTML diagnostics were removed.
- `platform-weread-contents-20260921T195523`: the existing landscape contents gate passed with this fix. **`platform-weread-contents-20260921T195730`: 1/1 passed**, with the strengthened portrait gate added: intro commit is 64 glyphs, then the requested first-chapter commit is 385 glyphs with its actual heading; landscape intro navigation and already-visible right-column selection also pass. Reviewed both exact portrait chapter captures. A real WebKit regression requires the title control to receive exactly one click with valid coordinates.

## Final verification

- **`platform-contracts-20260921T195948`: 276/276 passed, zero failures and zero skips.** Eight suites cover Google/Kobo contracts and live-WebKit fixtures, shared resume including Kindle compatibility, WeRead opening/availability and actual TOC events, and appearance. `PaymentTests` and optional external-file/network-only cases were not run.
- Final normally signed Release compilation and `scripts/verify-ipad-artifact.py` pass: universal iPhone/iPad family, four iPad orientations, scene support, both extensions, runtime locales, packaged Web resources, existing private Keychain group and no Debug acceptance markers. Binary SHA-256: `e0ff9bb73a63976037eeadfe3278abd9737f4b1d189af80b9b943589968f7537`.
- Installed `platform-final-candidate/CastReader.app` on the same 11-inch simulator and launched with no test arguments. The real saved WeRead first chapter restores without the earlier mismatch notice. The original `final-candidate/` from the initial iPad delivery remains separate.
- Final installed Release smoke: real WeRead Read resumed successfully, orange sentence highlighting advanced through the original text, portrait → landscape preserved live playback, and pausing then returning to portrait preserved visible highlighting. Returned to the signed-in Home with the existing Kindle/Google/Kobo shelves and paused mini-player. Screenshots are retained in `platform-final-candidate/`. Only the authorized 11-inch simulator is booted; no login reset or second device was used.
