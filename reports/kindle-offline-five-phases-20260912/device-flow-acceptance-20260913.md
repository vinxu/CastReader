# Kindle 离线链路真机验收 — 2026-09-13

本轮在 iPhone 15 Pro Max 上通过 iPhone 屏幕镜像验证真实 Kindle 页面。源码基于 `358efa8`，包含已验证发布祖先 `b6dd4d7`；使用隔离 DerivedData 和指定基线脚本构建，未发布或提交审核。

## 已验证的数据链路

- 427 页《A Journey to the Centre of the Earth》两次整本保存均完成，所有资源哈希通过独立导出检查，源范围从 0 连续覆盖至 480607。下载结果为 427 张图片、0 个 OCR 文本字符；OCR 在朗读时才执行。
- 两轮均与前次导出逐页比对：427 页源位置一致，图像全部通过现有像素等价标准。容差为单通道差异不超过 1/255、变化像素比例不超过 0.1%；不将此称为二进制完全相同，也不据此保证所有其他书籍均无缺页。
- 用户原有的 427 页离线书保持同一代次，全部页面记录未变化；测试副本使用单独目录。
- 正式“更多 → 离线保存整本书”入口保存《Donna Paola》，取消后保留 43 页并恢复在线位置。可以阅读已保存的图片，页码跳转限定在已缓存范围。
- 再次继续下载后补齐为 55 页。前 43 页记录逐项完全相同、代次不变，全部资源哈希与 0–56300 连续覆盖通过检查。文库显示“整本已保存 · 55 页”，再次打开恢复第 43 页。

## 朗读验证

新保存的英文纯图片副本首次打开时识别次数为 0。开始播放后执行端上 OCR，Samantha 系统朗读进入 speaking 状态并产生词范围回调，橙色高亮可见。前台从第 249 页连续推进；退到手机桌面后诊断仍为 background/speaking，并继续识别后续页面，返回显示第 254 页。

这验证了镜像连接时的本地 OCR / 系统朗读路径和应用后台推进。该轮网络状态提示仍为在线，不能作为严格飞行模式的通过证据，也未用“应用退到后台”代替手机实际锁屏验收。本轮实际音质/声画同步仍待用户听感确认。

## 实测速度与本轮优化

| 指标 | 优化前 | 优化后 |
|---|---:|---:|
| 427 页从创建到完成恢复 | 189.535 秒 | 239.261 秒 |
| 图片采集阶段合计 | 168.153 秒 | 231.337 秒 |
| 本地保存阶段合计 | 16.952 秒 | 3.859 秒 |
| 前 50 页保存中位数 | 9 毫秒 | 5 毫秒 |
| 最后 53 页保存中位数 | 74 毫秒 | 12 毫秒 |
| 超过 2 秒的单页采集 | 15 次 | 28 次 |

本地保存减少约 77%，但实时 Kindle 翻页/图片就绪开销增加，本轮整本总耗时反而更长。两次的网页缓存、网络和就绪情况并非受控一致，不能宣称整本加速，更不能承诺 30–60 秒完成。这仍是未达到的目标。

优化只复用一份已验证清单：每次读取仍读取磁盘真实字节，只有 URL 与全部字节完全相同才复用结果。外部改变哪怕保持同样长度及修改时间，仍重新验证。原子写入成功后才更新复用值；页面顺序、资源绑定、读取时图像哈希、最终整本验证及代次检查均保留。

## 真机发现并修复的界面问题

从文库用 NavigationLink 打开离线书时，根界面的在线迷你播放器会覆盖离线播放和声音控件。文库入口改成与下载完成页一致的独立全屏阅读界面；关闭后回文库；继续下载先关闭阅读界面，待关闭完成再路由下载，避免两层界面竞争。

已新增根界面存在迷你播放器的回归场景，并更新取消续传后的返回路径测试。6 个 UI 用例全部通过。真机用原在线会话复现相同前置条件后，确认离线书全屏展示、声音及语速菜单可操作、播放高亮正常，旧播放器不再遮挡。

## 飞行模式现场发现的入口问题

第一次用户开启飞行模式后反馈“播放没有反应”，随后指出手机显示的是在线朗读界面。重连镜像确认页面有 Read Aloud / Explain，在线日志记录云端声音失败；离线第 47 页诊断仍为 idle，没有新 OCR 或播放回调。这次测试不接受为离线通过，也不能据此断言本地 OCR/TTS 故障。

重新从文库打开第 47 页，复验“离线界面 → 手机桌面 → 应用图标返回”，仍保持离线界面。尚未确定第一次切到在线入口的具体操作；已请求在顶部“整本离线 / 第 47 / 55 页”得到确认后重测。入口混淆作为验收记录保留，不归咎于用户。

## 验证记录与边界

- 保存相关 50 项测试通过；优化后的第一轮 5 个完整 UI 用例通过。首次同长度篡改测试的夹具遗漏两个字段导致长度断言失败，补齐夹具字段后 50 项全部通过；产品对篡改的拒绝没有失效。
- 修复遮挡后的 6 项 UI 用例全部通过，最终 Release 编译通过，真机安装与遮挡复验通过。本轮相关检查合计 50 项单元回归 + 6 项 UI = 56 项通过。
- 原始书页图片、OCR 内容和设备原始日志仅在本机 `/tmp/CastReaderOfflineFlow-*` 留存，本报告只保存指标。
- 严格飞行模式（包括 Wi-Fi 关闭）、本轮听感、实际手机锁屏音频仍未验收；镜像依赖连接，断网后的物理操作需要设备旁的人完成。

详细字段、源文件哈希、测试结果和导出检查见 [device-flow-validation-20260913.json](device-flow-validation-20260913.json)。
